---
title: 第五章 特征值和特征向量 — 习题详解
date: 2026-06-07
status: Q1-Q24 全部完成 ✅
---

# 第五章 特征值和特征向量 · 习题详解

## 习题进度

已全部完成 Q1-Q24。以下仅记录**需要重点回顾的关键题**。

---

## Q17 · 实对称相似充要条件 ⭐

> A = [[1,a,1],[a,b,a],[1,a,1]] 与 B = diag(2,b,0) 相似的充要条件为（ ）。
> (A) a=0, b=2  (B) a=0, b任意  (C) a=2, b=0  (D) a=2, b任意
> 答案：B

**核心知识点**：实对称矩阵相似 ⇔ 特征值相同。
因为实对称必可对角化（几何重数=代数重数），特征值同 → 对角化后矩阵同 → 相似。

**五步解法**（逐个验证，不解三次方程）：

1. 识别 A 为实对称 → 特征值必须 = B 的特征值 2, b, 0
2. λ=0：A 第1行=第3行 → |A|=0 → 0是特征值 ✅（不卡参数）
3. λ=2：|A-2E| = 4a² = 0 → a = 0
4. λ=b（a=0代入）：|A-bE| 中间行全0 → 行列式恒0 → b任意
5. 答案：B

**关键计算**：|A-2E| 的行变换 → 第1行+第3行 → (0,2a,0) → 按第1行展开 → 4a²=0。

---

## Q23 · 线性方程组 + 正交对角化 ⭐

> A = [[1,1,a],[1,a,1],[a,1,1]]，β=(1,1,-2)ᵀ，Ax=β 有解但不唯一。
> (1) 求 a；(2) 求正交矩阵 Q 使 QᵀAQ 为对角矩阵。
> 答案：(1) a=-2

**关键陷阱**：|A|=0 解得 a=1 或 a=-2。

**a=1 为什么被排除**？
代入 A：三行全是 (1,1,1)。方程组变成：
```
x₁+x₂+x₃ = 1    ← 第1行
x₁+x₂+x₃ = 1    ← 第2行
x₁+x₂+x₃ = -2   ← 第3行：左边一样，右边是-2
```
同一表达式同时=1和=-2 → **矛盾，无解**。不符合"有解但不唯一"。

a=-2 时 r(A)=r(A,β)=2<3，无穷多解 ✅。

---

## Q24 · 正交性证明（"插桥法"）⭐

> 设 A 实对称，λ₁≠λ₂，证明 α₁ᵀα₂ = 0。

**核心技巧**：插入 A 作为桥——α₁ᵀAα₂ 同时连接 A、α₁、α₂。

```
第一次（A 作用在 α₂）：α₁ᵀAα₂ = λ₂(α₁ᵀα₂)
第二次（Aᵀ=A 翻到左边）：α₁ᵀAα₂ = λ₁(α₁ᵀα₂)

λ₂(α₁ᵀα₂) = λ₁(α₁ᵀα₂) → (λ₂-λ₁)(α₁ᵀα₂) = 0
λ₁≠λ₂ → α₁ᵀα₂ = 0。∎
```

---

## 实对称正交对角化 · 几何意义

**椭球模型**：
- 实对称矩阵 = 沿相互垂直方向的拉伸/压缩（纯变形，无旋转）
- 特征向量 = 椭球主轴方向（天然正交 = α₁ᵀα₂=0 的几何含义）
- 特征值 = 各方向的拉伸倍数
- 重特征值 = 截面是圆（任意方向都是特征向量，需 Schmidt 挑正交）
- 非对称矩阵 = 有旋转+剪切 → 特征向量不正交，可能有复数特征值

---

## 计算提速技巧

### 含 λ 行列式化简四招

| 招数 | 操作 | 适用场景 |
|------|------|---------|
| 提公因子 | 某行/列有公因子先提出来 | 行元素有大公约数 |
| 两行相减 | 行i - 行j → 出现零或公因子 | 两行结构类似 |
| 消元 | 某行 + k×另一行 | 消去含λ项 |
| 特殊公式 | 主对角全a其余b型直接用公式 | A=[[1,2,2],[2,1,2],[2,2,1]] |

### 特征向量求解四步

```
① λ₀ 代入 A-λE → 数字矩阵
② 化为行最简形
③ 自由变量个数 = n - 秩
④ 依次令自由变量=1，解基础解系
```

---

## 全章知识串联

从 Q4→Q5→Q6→Q7→Q17→Q24，每个知识点互相支撑：

```
Aα=λα（特征值定义）
  ↓
特征值变换表（Q4）—— P⁻¹AP 特征向量 = P⁻¹α
  ↓
对角化条件（Q5）—— n个线性无关特征向量
  ↓
实对称特殊性质（Q7）—— 必可对角化、特征向量正交
  ↓
实对称相似充要条件（Q17）—— 特征值相同⇔相似
  ↓
正交性证明（Q24）—— 插A做桥，λ₂-λ₁法
```

---

## 章节测试 · 2026-06-10 进度

| Q | 主题 | 结果 | 关键记录 |
|---|------|------|---------|
| Q1 | 特征值变换表 (1/3 A²)⁻¹ | | |
| Q2 | 伴随+矩阵多项式特征值 | | |
| Q3 | ααᵀ 特征值+不可逆判定 | | |
| Q4 | 可相似对角化判别 | ✅ | 四个上三角，特征值同(1,1,2)。判λ=1几何重数→rank(A-E)=1→选C |
| Q5 | 特征向量反求参数 | ⚠️→✅ | 初答没思路，纠正：Aα=λα 代入解方程 |
| Q6 | \|A\|=0+特征值不等→r(A) | ✅ | \|A\|=0→λ=0；互不相等→可对角化；r(Λ)=r(A)=2 |
| Q7 | 矩阵多项式→特征值 | ⚠️→✅ | (A+3E)(A-E)=0→特征值λ：(λ+3)(λ-1)=0→λ=-3或1，迹=-5→-3,-3,1 |
| Q8 | 相似+行列式综合 | ✅ | A~B→特征值同+行列式同 |
| Q9 | 4阶反对角矩阵特征值 | ✅→妙招 | 块对角化→两个2×2 [[0,1],[1,0]]→λ=±1各二重 |
| Q10 | 特征向量反求参数(3阶) | ✅ | 照搬Q5思路 Aα=λα
