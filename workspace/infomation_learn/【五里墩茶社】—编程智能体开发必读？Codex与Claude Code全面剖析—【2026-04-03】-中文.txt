同样是让AI帮你写代码
一个用rust写了80一个creat
e
靠操作系统内核来保证安全
另一个用typescript做了40
多个工具
靠大热模型分类器来判断
你的命令危不危险
大家好
我是小木头
今天我们来做一件事
把OpenAI的codex和atro
pic coo code
这两个当下最具代表性的AI编程智能体
从源码层面拆开
看看他们到底是怎么设计和实现的
我把这些分析整理成了一个开源项目
链接呢在描述中
代码仓库在这里我还将其整理成了一个
更方便阅读的网页
供大家参考
链接也在代码仓库的描述这里
今天这期视频我们不深入每个细节
但是呢会帮大家一起通览整个架构
建立一个完整的认知框架
在展开细节之前
先说最核心的一点
这两个智能体走了完全不同的路线
codex zi的思路是
把一切都关在沙箱里
让模型在里面自由发挥
他用操作系统内核级别的沙箱来
限制AI能做什么模型
想跑什么命令都行
但操作系统会挡住危险操作
这种思路呢属于containment
first coo code思路相反
它在执行前会分析一切
有风险就先问你
它没有内核沙箱
而是靠一个大元模型分类器来判断
每条命令的风险
本质上就是用cloud自己来
审查cloud要执行的操作
安全的自动执行
危险的就弹框问你特别危险的
直接拒绝
先看看技术选型
codex zi用rust写
是一个包含了80一个create的一
个庞大的代码仓库
也被称为mono rappo
每一个模块制作清晰
包含了CRITUI
还有核心组件
沙箱环境执行策略等
这些都是独立的quit编译器
强制执行模块边界
最终编译出来的是一个静态链
接的原生二进制文件
通过NPM做分发
NPM包其实只是一个包装器
一个rapper检测你的平台
然后启动对应的rust2进制
而clock code用的是type
script单体包结构
它有一个自定义的ink风格
终端渲染器底层是react加上rea
ct调节器
不是NPM上的ink包
而是自己fork出的一套
现在推荐的安装方式是原生
二进制自动更新
接下来看最核心的部分
Agent loop
也就是智能体循环
本质上都是同一个模式
接受输入调用模型
执行工具把结果喂回去再重复
但实现方式会非常的不同
codex是基于turn的状态机
在整个执行中或代码层面呢
有turn这么一个概念
上下文呢也被称为turn
context
每一轮都有一个明确的turn
其中就包含了模型信息
刷箱策略权限配置
来看看它的一个基本流程
构建提示词
调用API流失返回提取工具
调用执行更新历史决定是否继续如此往复
它没有最大的轮次限制
只要有工具在执行就会继续
clock hold是
基于的generator做的流失的循
环
核心是一个异步的generator函
数
通过yield不断产出事件
它更流畅
没有硬性的轮次边界
在流失的传输过程中就开始执行工具
这里有一个很关键的技术点
两者都在API流失返回的过程
中就开始执行工具
而不是等到响应的结束
区别在于结果的同步时机
codex做法是把每个工具调用派
生成一个独立的工作或任务放进容器
用读写锁来控制并发
只读工具拿到读锁可以并行
而写入工具拿到写的锁
独占执行工具在流失传输期间就开始跑
但所有结果在流结束时统一的发出
也就是说轮次的边界是同步点
clock code用的是strea
ming to executor
状态机工具一从流中解析出来就立刻执行
只读工具
最多能够做十个并行
写入工具是串行的
而不同的是
它可以在其他工具还在跑的时候
就通过yield发出
已经完成的结果
它的同步力度呢更细
没有硬性的轮次边界工具系统的设计是
另一个重大的不同
codex的工具比较少
大约有15个handler
核心就是shell以及apply
patch
他的哲学是模型
知道怎么用shell
给他一个shell就够了
想搜索文件模型自己跑RG
想看目录结构就跑find
所有操作都通过shell进入
内核的沙箱
而clock code工具会多一些
目测呢有40多个专用的工具
文件搜索有globe
tour和grab tour
文件编辑有发edit
它包含像web fetch
Web search
Lsp tool
agent tool等等
每个工具都有自己的输入校验权限
检查和输出的格式化
在系统提示词中还甚至明确的说了
永远不要用BH跑grab
要用grab t
在这个网页上也对工具呢做了比对
我们可以从不同的能力或维度来
看看codex以及clock
code
他们分别是怎么实现的
文件搜索是一个特别能体现
设计差异的例子
codex并不自带搜索工具
它的系统提示词里写着搜索文件和内容
优先用RG
如果RG没装
用其他的替代品模型
直接通过shell工具旁RG命令
如果用户机器上没有rip
grab模型
会受到command not
found这类错误
然后自动降级到grab或者find
而clock code自
带了rip grab
他把rip grab2进制打包
进了整个应用程序
要么编译进bomb2进制
要么作为vendor2进制按平台分发
grab tour是一个
完整的REGREP封装
值得注意的是
两者呢都没有做文件的索引
没有AST解析
没有符号表
没有搜索数据库
每次都是从头遍历文件系统根源
还在于rip grab足够快
大部分项目基本上一秒就能搜完
编辑文件的方式也不同
两个工具完全都是自研的
没有用操作系统的patch
set或get apply
codex
用到了apply patch
这是一个纯rust实现的补丁引擎
它不是标准的unify deef
它有一个4G模糊匹配算法
来定位代码的位置
精确匹配
忽略尾部空白
忽略首尾空白
UNICO标点归一化
一个工具调用可以同时的修改
创建删除多个文件
而且是事务性的
Clock code
用file edit
这是一个纯粹的typescript实
现
用来做字符串的替换
底层就是JAVASCRIPT原生的s
tring replace以及str
ing replace all
但是加了一层智能的引号归一化
包含了比如弯引号
直引号的相互匹配
还包含了编码的检测和换行符保留等等
一次调用只改一个文件的一个位置
简单可靠
安全是这两个智能体之间最大的架构差异
codex做的是内核级的沙箱
在Mac os以及LINUX或win
dows用到了不同的底层的基础
设施或者技术
codex提供了三种模式
read only是默认的
只能读不能写
Workspace right
允许在工作目录写入
实际上就是关闭沙箱
而clock code做的是
应用级权限控制
没有内核沙箱
它靠一个大圆模型分类器
实际上就是额外调一次cw API
让模型自己判断这条命令安不安全
这不是传统意义上的模型
而是用了大约模型来做审查操作
再加上路径白名单
黑名单加上用户交互式审批的对话框
五个来源的规则层层叠加
还有远程管理的企业级设置推送在网页
这里呢有第七章节
沙箱和安全
我也将两者在不同方面的实现做了比对
codex的方式更强
但更加不灵活
需要操作系统级别的支持
每个平台实现都略有不同
而clock code的方式更灵活
但理论上会更弱一些
如果分类器漏判了一个命令
就没有后背的防线了
随着对话变长
上下文窗口会被填满
两者都需要压缩策略
codex比较简单
他用ghost net shot标记
对旧消息做摘要
可以调用远程API来
压缩coo code
有五层
压缩策略
包括了上下文折叠
响应式压缩
主动压缩剪切压缩和未压缩
另外还有一个token预算机制
当模型认为自己完成了
但预算还没用完时
系统会注入一条继续的消息
让他接着干
如果连续三次催促模型
每次都只产出不到500个token
系统会判定智能体在空转提前退出
在模型集成方面
codex用的是open e i
responses API
既支持SSE
也支持web socket
Websocket
有连接尺和预热
而clock code使用的是ath
ropic message API
它只有SSE
但是呢有完善的prompt缓存机制
把系统提示词分为了可缓存的静态
部分和每轮更新的动态部分
这能够节省到不少的成本
多智能体架构也很不同
codex的子智能体是
进程内的共享内存
通过mailbox异步通道通信
因此可以fork负极的完整对话历史
而clock code它的子
智能体是独立进程
每个子智能体拿到一个任务的描述
从零开始没有负极的历史
可以选择在git work
tree隔离执行
总结一下吧
这两个智能体代表了两种工程哲学
codex走的是偏系统工程路线
利用rust基于内核沙箱类型化的协议
清晰的模块边界来进行开发和工程化
工具少
但是非常灵活安全
靠硬件来隔离
而clock code走的是产品路线
基于typescript
由大源模型驱动权限系统具有
丰富的工具生态
企业级的管理能力
工具多且专业安全
靠分析和审批
但核心循环是一样的
都是流式的调用模型
执行工具未回结果循环
不同的是围绕这个循环的所有工程的决策
我将这些内容呢整理
成了目前的12个章节
都放在了GITHUB
当前这个页面给到了大家一个
交互式的浏览体验
希望呢能给到大家一些帮助
如果你对AI编程智能体
对底层实现感兴趣呢
欢迎来看看好了
今天的分享就到这里
感谢大家收看
我们下期再见
