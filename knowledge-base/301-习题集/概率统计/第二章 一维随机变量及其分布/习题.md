---
title: 习题
date: 2026-05-22T10:13:06+08:00
lastmod: 2026-05-22T10:13:06+08:00
---

# 习题

## 第 297 页

第二章 一维随机变量及其分布

1、下列函数中，可以作为随机变量分布函数的是（ ）。  
(A)

$$
F(x) = \frac{1}{1+x^2}
$$

(B)

$$
F(x) = \frac{3}{4} + \frac{1}{2\pi} \arctan x
$$

(C)

$$
F(x) = \begin{cases} 0, & x \leqslant 0 \\ \frac{x}{1+x}, & x > 0 \end{cases}
$$

(D)

$$
F(x) = \frac{2}{\pi} \arctan x + 1
$$

2、设随机变量 $X$ 的分布函数为

$$
F(x) = \begin{cases} 0, & x < 0 \\ \frac{1}{2}, & 0 \leqslant x < 1 \\ 1-e^{-x}, & x \geqslant 1 \end{cases}
$$

则

$$
P\{X=1\} = \_\_\_\_\_.
$$

3、设随机变量 $X$ 的分布函数为 $F(x)$，试表示下列概率。  
(1)

$$
P\{a < X \leqslant b\}
$$

； (2)

$$
P\{a < X < b\}
$$

； (3)

$$
P\{a \leqslant X \leqslant b\}
$$

。

4、设随机变量 $X$ 的分布函数为

$$
F(x) = \begin{cases} c, & x \leqslant 0 \\ a + \frac{b}{(1+x)^2}, & x > 0 \end{cases}
$$

求常数 $a, b, c$ 的值。

## 第 298 页

5、设随机变量 $X$ 的分布函数为

$$
F(x) = \begin{cases} 
0, & x < 0, \\ 
A \sin x, & 0 \leqslant x \leqslant \frac{\pi}{2}, \\ 
1, & x > \frac{\pi}{2}, 
\end{cases}
$$

求概率 $P\{|X| < \frac{\pi}{6}\}$。

6、设离散型随机变量 $X$ 的分布律为

$$
\begin{array}{c|ccc} 
X & 1 & 2 & 3 \\ 
\hline 
P & 0.5 & 0.3 & 0.2 
\end{array}
$$

求 $X$ 的分布函数。

7、设随机变量 $X$ 的分布函数为

$$
F(x) = \begin{cases} 
0, & x < -1, \\ 
0.4, & -1 \leqslant x < 1, \\ 
0.8, & 1 \leqslant x < 3, \\ 
1, & x \geqslant 3, 
\end{cases}
$$

求 $X$ 的分布律。

8、设随机变量 $X$ 的概率密度函数为

$$
f(x) = \begin{cases} 
\frac{2}{\pi \sqrt{1-x^2}}, & 0 < x < C, \\ 
0, & \text{其他}. 
\end{cases}
$$

(1) 确定常数 $C$ 的值；  
(2) 求 $X$ 的分布函数。

## 第 299 页

9、设随机变量 $X$ 的概率密度函数为

$$
f(x) = \begin{cases} 
x, & 0 \leqslant x < 1, \\ 
2-x, & 1 \leqslant x < a, \\ 
0, & \text{其他}. 
\end{cases}
$$

(1) 确定常数 $a$ 的值；  
(2) 求 $X$ 的分布函数。

10、设 $X_1, X_2$ 为任意两个连续型随机变量，它们的分布函数分别为 $F_1(x)$ 和 $F_2(x)$，它们的概率密度函数分别为 $f_1(x)$ 和 $f_2(x)$，则（　）。  
(A) $F_1(x) + F_2(x)$ 必为某随机变量的分布函数  
(B) $F_1(x) - F_2(x)$ 必为某随机变量的分布函数  
(C) $f_1(x) \cdot f_2(x)$ 必为某随机变量的概率密度函数  
(D) $\frac{1}{3} f_1(x) + \frac{2}{3} f_2(x)$ 必为某随机变量的概率密度函数

11、设随机变量 $X$ 的概率密度函数为 $f(x)$，且 $f(x) = f(-x)$，$F(x)$ 为 $X$ 的分布函数，则对于任意实数 $a$，有（　）。  
(A) $F(-a) = 1 - \int_0^a f(x) dx$  
(B) $F(-a) = \frac{1}{2} - \int_0^a f(x) dx$  
(C) $F(-a) = F(a)$  
(D) $F(-a) = 2F(a) - 1$

12、设随机变量 $X$ 服从二项分布 $B(2, p)$，随机变量 $Y$ 服从二项分布 $B(3, p)$，若 $P\{X \geqslant 1\} = \frac{5}{9}$，则 $P\{Y \geqslant 1\} =$ ______。

13、设随机变量 $X$ 服从二项分布 $B(3, p)$，且 $P\{X=0\} = P\{X=1\}$，

## 第 300 页

则 $P\{X=2\} =$ ______。

14、设离散型随机变量 $X$ 的分布律为

$$
\begin{array}{c|ccc} 
X & 0 & 1 & 2 \\ 
\hline 
P & \frac{1}{3} & \frac{1}{6} & \frac{1}{2} 
\end{array}
$$

现对 $X$ 进行 3 次独立观测，求至少有 2 次观测值大于 1 的概率。

15、设某段时间内通过路口的车流量 $X$ 服从参数为 $\lambda$ 的泊松分布，已知该时段没有车通过的概率为 $\frac{1}{e}$，则该时段至少有 2 辆车通过的概率为 ______。

16、设离散型随机变量 $X$ 的分布律为 $P\{X=k\} = \frac{C}{k!} e^{-2}, k=0, 1, 2, \cdots$，则常数 $C =$ ______。

17、设随机变量 $K$ 的取值为区间 $[0, 5]$ 上的随机点，求关于 $x$ 的方程 $4x^2 + 4Kx + K + 2 = 0$ 有实根的概率。

18、设银行办理某项业务的时间 $X$（分钟）服从参数为 $\frac{1}{10}$ 的指数分布，若你去银行办理该项业务时，前面有一个人刚开始办理，且只有 1 个窗口开放，求你等待 10 分钟到 20 分钟的概率。

19、设随机变量 $X$ 服从正态分布 $N(\mu, \sigma^2)$，则 $P\{|X - \mu| \leqslant 3\sigma\} =$ ______。  
参考数据：$\Phi(3) = 0.9987$。

## 第 301 页

20. 设随机变量$X$和$Y$均服从正态分布，$X \sim N(\mu, 4^2)$，$Y \sim N(\mu, 5^2)$，记$P_1 = P\{X \leqslant \mu - 4\}$，$P_2 = P\{Y \geqslant \mu + 5\}$，则( ).  
    (A) 对任何实数$\mu$，都有$P_1 = P_2$  
    (B) 对任何实数$\mu$，都有$P_1 < P_2$  
    (C) 对某些实数$\mu$，有$P_1 = P_2$  
    (D) 对任何实数$\mu$，都有$P_1 > P_2$
21. 设离散型随机变量$X$的分布律为

$$
\begin{array}{c|c|c|c|c}
X & -1 & 0 & 1 & 2 \\
\hline
P & \frac{1}{3} & \frac{1}{4} & \frac{1}{4} & \frac{1}{6}
\end{array}
$$

随机变量$Y = X^2$，求$Y$的分布律.

22. 设随机变量$X$服从正态分布$N(\mu, \sigma^2)$，令$Y = \frac{X-\mu}{\sigma}$，证明：随机变量$Y$服从标准正态分布$N(0, 1)$.
23. 设随机变量$X$在$(0, 2)$内服从均匀分布，令$Y = X^2$，求随机变量$Y$的概率密度函数.
24. 设随机变量$X$的概率密度函数为  
    $f_X(x) = \begin{cases} e^{-x}, & x \geqslant 0, \\ 0, & x < 0, \end{cases}$  
    令$Y = e^X$，求随机变量$Y$的概率密度函数.
