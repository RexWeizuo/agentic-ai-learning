---
title: 答案
date: 2026-05-22T10:13:07+08:00
lastmod: 2026-05-22T10:13:07+08:00
---

# 答案

## 第 302 页

参考答案

1. 解析  
   对于(A)，因为$F(+\infty) = 0 \neq 1$，且$F(x)$不满足单调不减，所以排除(A)；  
   对于(B)，因为$F(-\infty) = \frac{1}{2} \neq 0$，所以排除(B)；  
   对于(D)，因为$F(+\infty) = 2 > 1$，所以排除(D)；  
   故本题选(C).

小崔点睛：$F(x)$作为分布函数要满足4条性质，①单调不减；②右连续；③$0 \leqslant F(x) \leqslant 1$；④$F(-\infty)=0, F(+\infty)=1$.

2. 解析  
   $P\{X = 1\} = P\{X \leqslant 1\} - P\{X < 1\} = F(1) - F(1^-)$  
   $= 1 - e^{-1} - \frac{1}{2} = \frac{1}{2} - e^{-1}$.

小崔点睛：$P\{X \leqslant a\} = F(a)$，$P\{X < a\} = F(a^-)$.

3. 解析  
   (1) $P\{a < X \leqslant b\} = P\{X \leqslant b\} - P\{X \leqslant a\} = F(b) - F(a)$.  
   (2) $P\{a < X < b\} = P\{X < b\} - P\{X \leqslant a\} = F(b^-) - F(a)$.  
   (3) $P\{a \leqslant X \leqslant b\} = P\{X \leqslant b\} - P\{X < a\} = F(b) - F(a^-)$.
4. 解析  
   由$F(-\infty) = c = 0$可知$c = 0$，  
   由$F(+\infty) = \lim_{x \to +\infty} \left[ a + \frac{b}{(1+x)^2} \right] = a = 1$可知$a = 1$，

## 第 303 页

又$F(x)$在$x=0$处右连续，则$F(0^+) = F(0)$，  
$\lim_{x \to 0^+} \left[ 1 + \frac{b}{(1+x)^2} \right] = 1 + b = 0$，可知$b = -1$，  
综上，$a = 1$，$b = -1$，$c = 0$.

5. 解析  
   由$F(x)$在$x = \frac{\pi}{2}$处右连续可得$F(\frac{\pi}{2}) = F(\frac{\pi}{2}^+)$，  
   即$A \sin \frac{\pi}{2} = 1$，得$A = 1$，  
   则$P\{|X| < \frac{\pi}{6}\} = P\{-\frac{\pi}{6} < X < \frac{\pi}{6}\} = P\{X < \frac{\pi}{6}\} - P\{X \leqslant -\frac{\pi}{6}\}$  
   $= F(\frac{\pi}{6}^-) - F(-\frac{\pi}{6}) = \sin \frac{\pi}{6} - 0 = \frac{1}{2}$.
6. 解析  
   $F(x) = P\{X \leqslant x\} = \begin{cases} 0, & x < 1, \\ 0.5, & 1 \leqslant x < 2, \\ 0.8, & 2 \leqslant x < 3, \\ 1, & x \geqslant 3. \end{cases}$
7. 解析

$$
\begin{array}{c|c|c|c}
X & -1 & 1 & 3 \\
\hline
P & 0.4 & 0.4 & 0.2
\end{array}
$$

8. 解析  
   (1) 由$\int_{-\infty}^{+\infty} f(x) dx = \int_{0}^{C} \frac{2}{\pi \sqrt{1-x^2}} dx = \frac{2}{\pi} \arcsin x \Big|_0^C = \frac{2}{\pi} \arcsin C = 1$可知，  
   $\arcsin C = \frac{\pi}{2}$，则$C = 1$.

## 第 304 页

(2) ①当$x < 0$时，$F(x) = 0$；  
②当$0 \leqslant x < 1$时，$F(x) = \int_{-\infty}^{x} f(t)dt = \int_{0}^{x} \frac{2}{\pi \sqrt{1-t^2}} dt = \frac{2}{\pi} \arcsin x$，  
③当$x \geqslant 1$时，$F(x) = 1$。  
综上，$F(x) = \begin{cases} 0, & x < 0 \\ \frac{2}{\pi} \arcsin x, & 0 \leqslant x < 1 \\ 1, & x \geqslant 1 \end{cases}$

9. 解析  
   (1) 因为 $\int_{-\infty}^{+\infty} f(x)dx = \int_{0}^{1} x dx + \int_{1}^{a} (2-x)dx = 2a - \frac{1}{2}a^2 - 1 = 1$，  
   所以 $a = 2$。  
   (2) $F(x) = P\{X \leqslant x\} = \int_{-\infty}^{x} f(t)dt$。  
   ①当$x < 0$时，$\{X \leqslant x\}$是不可能事件，则$F(x) = 0$；  
   ②当$0 \leqslant x < 1$时，$F(x) = \int_{0}^{x} t dt = \frac{1}{2}x^2$；  
   ③当$1 \leqslant x < 2$时，$F(x) = \int_{0}^{1} t dt + \int_{1}^{x} (2-t)dt = -\frac{1}{2}x^2 + 2x - 1$；  
   ④当$x \geqslant 2$时，$\{X \leqslant x\}$是必然事件，则$F(x) = 1$。  
   综上，$F(x) = \begin{cases} 0, & x < 0 \\ \frac{1}{2}x^2, & 0 \leqslant x < 1 \\ -\frac{1}{2}x^2 + 2x - 1, & 1 \leqslant x < 2 \\ 1, & x \geqslant 2 \end{cases}$
10. 解析  
    对于(A)，因为 $\lim_{x \to +\infty} [F_1(x) + F_2(x)] = 1 + 1 = 2 \neq 1$，所以排除(A)；  
    对于(B)，因为 $\lim_{x \to +\infty} [F_1(x) - F_2(x)] = 1 - 1 = 0 \neq 1$，所以排除(B)；

## 第 305 页

对于(C)，举反例 $f_1(x) = \begin{cases} 1, & 0 < x < 1 \\ 0, & \text{其他} \end{cases}$，$f_2(x) = \begin{cases} 1, & 1 < x < 2 \\ 0, & \text{其他} \end{cases}$，  
则 $f_1(x)f_2(x) \equiv 0$，显然不是概率密度函数，所以排除(C)；  
对于(D)，因为 $\frac{1}{3}f_1(x) + \frac{2}{3}f_2(x) \geqslant 0$，  
又 $\int_{-\infty}^{+\infty} [\frac{1}{3}f_1(x) + \frac{2}{3}f_2(x)] dx = \frac{1}{3}\int_{-\infty}^{+\infty} f_1(x)dx + \frac{2}{3}\int_{-\infty}^{+\infty} f_2(x)dx$

$$
= \frac{1}{3} + \frac{2}{3} = 1
$$

，  
故本题选(D)。

小雀点睛：$f(x)$作为概率密度函数要具有2条性质，①$f(x) \geqslant 0$；  
②$\int_{-\infty}^{+\infty} f(x)dx = 1$。

11. 解析  
    由$f(x) = f(-x)$可知概率密度函数$f(x)$为偶函数，  
    所以画出右侧示意图，由对称性可知，  
    $F(-a) + \int_{0}^{a} f(x)dx = \frac{1}{2}$，所以(A)错误，(B)正确，  
    故本题选(B)。  
    对于(C)，正确关系应为$F(a) + F(-a) = 1$，  
    对于(D)，正确关系应为$2F(a) - 1 = \int_{-a}^{a} f(x)dx$。
12. 解析  
    因为$X \sim B(2, p)$，  
    所以$P\{X \geqslant 1\} = 1 - P\{X = 0\} = 1 - C_2^0 p^0 (1-p)^2 = 1 - (1-p)^2 = \frac{5}{9}$，  
    求得$P = \frac{1}{3}$，因而$Y \sim B(3, \frac{1}{3})$，  
    故$P\{Y \geqslant 1\} = 1 - P\{Y = 0\} = 1 - C_3^0 (\frac{1}{3})^0 (\frac{2}{3})^3 = \frac{19}{27}$。

## 第 306 页

小雀点睛：若$X \sim B(n, p)$，则$P\{X = k\} = C_n^k p^k (1-p)^{n-k}, k = 0, 1, \cdots, n$。

13. 解析  
    因为$X \sim B(3, p)$，又$P\{X = 1\} = P\{X = 0\}$，  
    所以$C_3^1 p (1-p)^2 = C_3^0 p^0 (1-p)^3$，解得$P = \frac{1}{4}$，  
    故$P\{X = 2\} = C_3^2 (\frac{1}{4})^2 \frac{3}{4} = \frac{9}{64}$。
14. 解析  
    对$X$的单次观测值大于1的概率$P\{X > 1\} = P\{X = 2\} = \frac{1}{2}$，  
    又对$X$的单次观测可以看成一次伯努利试验，因为试验结果只有两种：$X>1$发生或不发生。  
    所以设$Y$表示3次观测中$X>1$的次数，则$Y \sim B(3, \frac{1}{2})$，  
    故$P\{Y \geqslant 2\} = P\{Y = 2\} + P\{Y = 3\}$

$$
= C_3^2 (\frac{1}{2})^3 + C_3^3 (\frac{1}{2})^3 = \frac{1}{2}
$$

。

15. 解析  
    因为$X \sim P(\lambda)$，所以$P\{X = k\} = \frac{\lambda^k e^{-\lambda}}{k!}, k = 0, 1, 2, \cdots$，  
    则$P\{X = 0\} = \frac{\lambda^0 e^{-\lambda}}{0!} = e^{-\lambda} = \frac{1}{e}$，求得$\lambda = 1$，  
    故$P\{X \geqslant 2\} = 1 - P\{X = 0\} - P\{X = 1\}$

$$
= 1 - e^{-1} - \frac{1 \cdot e^{-1}}{1!} = 1 - \frac{2}{e}
$$

。

16. 解析  
    因为 $\sum_{k=0}^{\infty} P\{X = k\} = 1$，

## 第 307 页

所以 $\sum_{k=0}^{\infty} \frac{C \cdot e^{-2}}{k!} = C \cdot e^{-2} \sum_{k=0}^{\infty} \frac{1}{k!} = C \cdot e^{-2} \cdot e = 1$，

故 $C = e$。

小雀点睛：本题用了一个常考的数项级数和 $\sum_{k=0}^{\infty} \frac{1}{k!} = e$，推导也很容易，因为 $e^x = 1 + x + \frac{x^2}{2!} + \cdots$，令 $x = 1$ 就得到 $e = \sum_{k=0}^{\infty} \frac{1}{k!}$。

17. 解析  
    由题意可知 $K \sim U[0, 5]$，  
    而原方程有实根意味着 $(4K)^2 - 4 \cdot 4(K+2) \geqslant 0$，  
    解得 $K \geqslant 2$ 或 $K \leqslant -1$。  
    又 $P\{K \geqslant 2 \text{ 或 } K \leqslant -1\} = P\{K \geqslant 2\} + P\{K \leqslant -1\} = \frac{3}{5} + 0 = \frac{3}{5}$，  
    故原方程有实根的概率为 $\frac{3}{5}$。
18. 解析  
    由题意得，X的概率密度函数为

$$
f(x) = \begin{cases} \frac{1}{10} e^{-\frac{x}{10}}, & x > 0, \\ 0, & x \leqslant 0, \end{cases}
$$

故 $P\{10 \leqslant X \leqslant 20\} = \int_{10}^{20} \frac{1}{10} e^{-\frac{x}{10}} dx = e^{-1} - e^{-2}$。

小雀点睛：考研人请注意，在各位复习时常用的一些大学教材中，指数分布的概率密度函数有两种常见形式，第一种为 $X \sim E(\lambda)$，当 $x > 0$ 时，$f(x) = \lambda e^{-\lambda x}$；第二种为 $X \sim E(\theta)$，当 $x > 0$ 时，$f(x) = \frac{1}{\theta} e^{-\frac{x}{\theta}}$。前者参数为 $\lambda$，后者参数为 $\theta$，其实二者没什么本质区别，无非是 $\lambda = \frac{1}{\theta}$，但考研命题需要一个确定的描述，所以考大纲明确规定用

## 第 308 页

$E(\lambda)$ 的形式，即若 $X$ 服从参数为 $\lambda (\lambda > 0)$ 的指数分布，则 $X$ 的概率密度函数为

$$
f(x) = \begin{cases} \lambda e^{-\lambda x}, & x > 0, \\ 0, & x \leqslant 0. \end{cases}
$$

19. 解析  
    因为 $X \sim N(\mu, \sigma^2)$，所以 $Y = \frac{X - \mu}{\sigma} \sim N(0, 1)$，  
    故 $P\{|X - \mu| \leqslant 3\sigma\} = P\{|\frac{X - \mu}{\sigma}| \leqslant 3\}$  
    $= P\{|Y| \leqslant 3\} = 2\Phi(3) - 1$  
    $= 2 \times 0.9974 - 1 = 0.9974$。  
    (此处配图为标准正态分布曲线图，阴影部分为 $[-3, 3]$)

小雀点睛：若 $X \sim N(0, 1)$，则 $P\{|X| \leqslant a\} = 2\Phi(a) - 1$，推导如下。  
由对称性可知，$2\Phi(a) = 2\int_{-\infty}^a \varphi(x) dx = \int_{-\infty}^{+\infty} \varphi(x) dx + \int_{-a}^a \varphi(x) dx = \int_{-\infty}^{+\infty} \varphi(x) dx + \int_{-a}^a \varphi(x) dx = 1 + \int_{-a}^a \varphi(x) dx = 1 + P\{|X| \leqslant a\}$，故 $P\{|X| \leqslant a\} = 2\Phi(a) - 1$。

20. 解析  
    因为 $X \sim N(\mu, 4^2)$，$Y \sim N(\mu, 5^2)$，  
    所以 $\frac{X - \mu}{4} \sim N(0, 1)$，$\frac{Y - \mu}{5} \sim N(0, 1)$。  
    则 $P_1 = P\{X \leqslant \mu - 4\} = P\{\frac{X - \mu}{4} \leqslant -1\} = \Phi(-1) = 1 - \Phi(1)$，  
    $P_2 = P\{Y \geqslant \mu + 5\} = P\{\frac{Y - \mu}{5} \geqslant 1\} = 1 - \Phi(1)$，  
    所以 $P_1 = P_2$，故本题选 (A)。

## 第 309 页

21. 解析

    |Y|0|1|4|
    | :-: | :-: | :-: | :-: |
    |P|$\frac{1}{4}$|$\frac{7}{12}$|$\frac{1}{6}$|
22. 解析  
    已知 $f_X(x) = \frac{1}{\sqrt{2\pi} \cdot \sigma} e^{-\frac{(x - \mu)^2}{2\sigma^2}}$，  
    $F_Y(y) = P\{Y \leqslant y\} = P\{\frac{X - \mu}{\sigma} \leqslant y\} = P\{X \leqslant \sigma y + \mu\} = F_X(\sigma y + \mu)$，  
    则 $f_Y(y) = \frac{d F_Y(y)}{dy} = \frac{d F_X(\sigma y + \mu)}{dy} = f_X(\sigma y + \mu) \cdot \sigma$  
    $= \frac{1}{\sqrt{2\pi} \cdot \sigma} e^{-\frac{(\sigma y + \mu - \mu)^2}{2\sigma^2}} \cdot \sigma$  
    $= \frac{1}{\sqrt{2\pi}} e^{-\frac{y^2}{2}}$，  
    故 $Y \sim N(0, 1)$。

小雀点睛：关于正态标准化，我已经做了非常详细的视频讲解，大家可以在我的B站主页搜索“正态标准化”进行学习。

23. 解析  
    已知 $f_X(x) = \begin{cases} \frac{1}{2}, & 0 < x < 2, \\ 0, & \text{其他}. \end{cases}$  
    因为 $X$ 仅有可能在 $(0, 2)$ 内取值，所以 $Y = X^2$ 在 $(0, 4)$ 内取值，  
    因此，当 $y \leqslant 0$ 时，$\{Y \leqslant y\}$ 是不可能事件，$F_Y(y) = P\{Y \leqslant y\} = 0$;

## 第 310 页

当 $y \ge 4$ 时，$\{Y \le y\}$ 是必然事件，$F_Y(y) = P\{Y \le y\} = 1$；  
当 $0 < y < 4$ 时，$F_Y(y) = P\{Y \le y\} = P\{X^2 \le y\} = P\{-\sqrt{y} \le X \le \sqrt{y}\}$

$$
= \int_{-\sqrt{y}}^{\sqrt{y}} f_X(x) dx = \int_0^{\sqrt{y}} \frac{1}{2} dx = \frac{1}{2}\sqrt{y}
$$

综上，$Y$ 的分布函数为

$$
F_Y(y) = \begin{cases} 0, & y < 0, \\ \frac{1}{2}\sqrt{y}, & 0 < y < 4, \\ 1, & y \ge 4. \end{cases}
$$

故 $Y$ 的概率密度函数为

$$
f_Y(y) = \begin{cases} \frac{1}{4\sqrt{y}}, & 0 < y < 4, \\ 0, & \text{其他}. \end{cases}
$$

24. 解析  
    因为 $X$ 仅有可能在 $[0, +\infty)$ 上取值，所以 $Y = e^X$ 在 $[1, +\infty)$ 上取值，  
    因此，当 $y < 1$ 时，$\{Y \le y\}$ 是不可能事件，$F_Y(y) = P\{Y \le y\} = 0$；  
    当 $y \ge 1$ 时，$F_Y(y) = P\{Y \le y\} = P\{e^X \le y\} = P\{X \le \ln y\}$

$$
= \int_0^{\ln y} e^{-x} dx = 1 - \frac{1}{y}
$$

综上，$Y$ 的分布函数为

$$
F_Y(y) = \begin{cases} 0, & y < 1, \\ 1 - \frac{1}{y}, & y \ge 1. \end{cases}
$$

故 $Y$ 的概率密度函数为

$$
f_Y(y) = \begin{cases} \frac{1}{y^2}, & y \ge 1, \\ 0, & \text{其他}. \end{cases}
$$
