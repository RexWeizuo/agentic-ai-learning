---
title: 答案
date: 2026-05-22T10:13:09+08:00
lastmod: 2026-05-22T10:13:09+08:00
---

# 答案

## 第 349 页

**参考答案**

1. 解析  
   统计量定义为“完全由样本定义的量”，也就是说，不能包含任何未知元素，(B) 选项中 $\sigma$ 未知，因而不是统计量。  
   故本题选 (B) .
2. 解析

$$
E(\bar{X}) = E\left[ \frac{1}{n}(X_1 + X_2 + \cdots + X_n) \right] = \frac{1}{n} [ E(X_1) + E(X_2) + \cdots + E(X_n) ]
$$

$$
= \frac{1}{n} \cdot n E(X) = E(X) = 2 ,
$$

$$
D(\bar{X}) = D\left[ \frac{1}{n}(X_1 + X_2 + \cdots + X_n) \right] = \frac{1}{n^2} [ D(X_1) + D(X_2) + \cdots + D(X_n) ]
$$

$$
= \frac{1}{n^2} \cdot n D(X) = \frac{1}{n} D(X) = \frac{16}{n} ,
$$

又 $X_1, X_2, \cdots, X_n$ 相互独立，则 $\frac{1}{n} \sum_{i=1}^n X_i = \bar{X} \sim N(2, \frac{16}{n})$ ，  
标准化后可知

$$
\frac{\bar{X} - 2}{\frac{4}{\sqrt{n}}} \sim N(0, 1) ,
$$

故本题选 (C) .

小雀点睛：$X_1, X_2, \cdots, X_n$ 是简单随机样本意味着 $X_1, X_2, \cdots, X_n$ 独立同分布于 $X$，则 $E(X_i) = E(X)$，$D(X_i) = D(X)$。

3. 解析  
   因为 $\bar{X} \sim N(\mu, \frac{\sigma^2}{n})$ ，  
   所以标准化有

$$
\frac{\bar{X} - \mu}{\frac{\sigma}{\sqrt{n}}} \sim N(0, 1) ,
$$

## 第 350 页

$$
\text{则 } P\{ |\bar{X} - \mu| < \sigma \} = P\left\{ \left| \frac{\bar{X} - \mu}{\frac{\sigma}{\sqrt{n}}} \right| < \sqrt{n} \right\}
$$

$$
= 2\Phi(\sqrt{n}) - 1 ,
$$

即只与 $n$ 有关，故本题选 (C) .

小雀点睛：若 $X \sim N(0, 1)$，则 $P\{|X| \le a\} = 2\Phi(a) - 1$，推导如下。由对称性可知，$2\Phi(a) = 2 \int_{-\infty}^{a} \varphi(x) dx = \int_{-a}^{+\infty} \varphi(x) dx + \int_{-\infty}^{a} \varphi(x) dx = \int_{-a}^{+\infty} \varphi(x) dx + \int_{-\infty}^{-a} \varphi(x) dx + \int_{-a}^{a} \varphi(x) dx = \int_{-\infty}^{+\infty} \varphi(x) dx + \int_{-a}^{a} \varphi(x) dx = 1 + P\{|X| \le a\}$，故 $P\{|X| \le a\} = 2\Phi(a) - 1$。

4. 解析  
   由题意得 $\bar{X} \sim N(\mu, \frac{\sigma^2}{n})$，$\bar{Y} \sim N(\mu, \frac{\sigma^2}{n})$，且 $\bar{X}$ 与 $\bar{Y}$ 相互独立，  
   则 $E(\bar{X} - \bar{Y}) = E(\bar{X}) - E(\bar{Y}) = 0$，$D(\bar{X} - \bar{Y}) = D(\bar{X}) + D(\bar{Y}) = \frac{2\sigma^2}{n}$，  
   可知 $\bar{X} - \bar{Y} \sim N(0, \frac{2\sigma^2}{n})$，标准化得

$$
\frac{\bar{X} - \bar{Y}}{\sqrt{\frac{2\sigma^2}{n}}} \sim N(0, 1) .
$$

所以 $P\{ |\bar{X} - \bar{Y}| > \sigma \} = P\left\{ \left| \frac{\bar{X} - \bar{Y}}{\sqrt{\frac{2\sigma^2}{n}}} \right| > \frac{\sigma}{\sqrt{\frac{2\sigma^2}{n}}} \right\}$

$$
= P\left\{ \left| \frac{\bar{X} - \bar{Y}}{\sqrt{\frac{2\sigma^2}{n}}} \right| > \sqrt{\frac{n}{2}} \right\}
$$

$$
= 1 - P\left\{ \left| \frac{\bar{X} - \bar{Y}}{\sqrt{\frac{2\sigma^2}{n}}} \right| \le \sqrt{\frac{n}{2}} \right\}
$$

$$
= 1 - [ 2\Phi(\sqrt{\frac{n}{2}}) - 1 ] = 2 - 2\Phi(\sqrt{\frac{n}{2}}) ,
$$

可见 $P\{ |\bar{X} - \bar{Y}| > \sigma \}$ 仅与 $n$ 有关，与 $\sigma$ 无关，  
故本题选 (C) .

## 第 351 页

5. 解析  
   因为 $X \sim N(\mu, \sigma^2)$，所以 $\bar{X} \sim N(\mu, \frac{\sigma^2}{n})$，  
   进而 $X - \mu \sim N(0, \sigma^2)$，$\bar{X} - \mu \sim N(0, \frac{\sigma^2}{n})$，  
   标准化得

$$
\frac{X - \mu}{\sigma} \sim N(0, 1), \quad \frac{\bar{X} - \mu}{\frac{\sigma}{\sqrt{n}}} \sim N(0, 1) ,
$$

则 $P_1 = P\{ |X - \mu| \le \varepsilon \} = P\left\{ \left| \frac{X - \mu}{\sigma} \right| \le \frac{\varepsilon}{\sigma} \right\}$，

$$
P_2 = P\{ |\bar{X} - \mu| \le \varepsilon \} = P\left\{ \left| \frac{\bar{X} - \mu}{\frac{\sigma}{\sqrt{n}}} \right| \le \frac{\varepsilon}{\sigma} \cdot \sqrt{n} \right\} ,
$$

因为 $n \ge 2$，所以 $\frac{\varepsilon}{\sigma} \cdot \sqrt{n} > \frac{\varepsilon}{\sigma}$，  
则 $P\left\{ \left| \frac{X - \mu}{\sigma} \right| \le \frac{\varepsilon}{\sigma} \right\} < P\left\{ \left| \frac{\bar{X} - \mu}{\frac{\sigma}{\sqrt{n}}} \right| \le \frac{\varepsilon}{\sigma} \cdot \sqrt{n} \right\}$，  
即 $P_1 < P_2$，故本题选 (B) .

6. 解析  
   画出 $\varphi(x)$ 的图像，它是偶函数。  
   因为 $P\{|X| < x\} = \alpha$，  
   所以 $P\{0 < X < x\} = \frac{\alpha}{2}$，又 $P\{X > 0\} = \frac{1}{2}$，  
   则 $P\{X \ge x\} = \frac{1}{2} - \frac{\alpha}{2}$，即 $x = u_{\frac{1-\alpha}{2}}$，  
   故本题选 (C) .
7. 解析  
   (1) 因为 $\bar{X} = \frac{1}{n} \sum_{i=1}^n X_i$，所以 $X_{n+1}$ 与 $\bar{X}$ 相互独立，

## 第 352 页

则 $E(X_{n+1} - \overline{X}) = E(X_{n+1}) - E(\overline{X}) = \mu - \mu = 0$ ,

$$
D(X_{n+1} - \overline{X}) = D(X_{n+1}) + D(\overline{X}) = \sigma^2 + \frac{\sigma^2}{n} = \frac{n+1}{n}\sigma^2
$$

 ,  
故 $X_{n+1} - \overline{X} \sim N(0, \frac{n+1}{n}\sigma^2)$ .  
(2) $E(X_1 - \overline{X}) = E(X_1) - E(\overline{X}) = \mu - \mu = 0$ ,  
因为 $X_1 - \overline{X} = X_1 - \frac{1}{n}(X_1 + X_2 + \cdots + X_n)$

$$
= (1 - \frac{1}{n})X_1 - \frac{1}{n}(X_2 + X_3 + \cdots + X_n)
$$

 ,  
所以 $D(X_1 - \overline{X}) = (1 - \frac{1}{n})^2 D(X_1) + \frac{1}{n^2}[D(X_2) + D(X_3) + \cdots + D(X_n)]$

$$
= (1 - \frac{1}{n})^2 \cdot \sigma^2 + \frac{1}{n^2} \cdot (n-1)\sigma^2 = \frac{n-1}{n}\sigma^2
$$

 .  
故 $X_1 - \overline{X} \sim N(0, \frac{n-1}{n}\sigma^2)$ .  
小雀点睛：注意 $D(X_1 - \overline{X}) \neq D(X_1) + D(\overline{X})$ , 因为 $\overline{X} = \frac{1}{n}(X_1 + X_2 + \cdots + X_n)$ ,显然不与 $X_1$ 独立，故要将 $X_1 - \overline{X}$ 拆开.

8. 解析

$$
kY = [\sqrt{k}(X_1 + X_2 + X_3)]^2 + [\sqrt{k}(X_4 + X_5 + X_6)]^2
$$

 ,  
因为 $kY$ 服从 $\chi^2$ 分布，  
所以 $\sqrt{k}(X_1 + X_2 + X_3) \sim N(0, 1)$ , $\sqrt{k}(X_4 + X_5 + X_6) \sim N(0, 1)$ .  
又 $X_1, X_2, \cdots, X_6 \sim N(0, 1)$ ,  
所以 $\sqrt{k}(X_1 + X_2 + X_3) \sim N(0, 3k)$ , $\sqrt{k}(X_4 + X_5 + X_6) \sim N(0, 3k)$ ,  
故 $3k = 1$ , 即 $k = \frac{1}{3}$ .

9. 解析

$$
Y = [\sqrt{a}(X_1 - 2X_2)]^2 + [\sqrt{b}(3X_3 - 4X_4)]^2
$$

 ,  
因为 $X_1, X_2, X_3, X_4 \sim N(0, 4)$ ,

## 第 353 页

所以 $E[\sqrt{a}(X_1 - 2X_2)] = \sqrt{a}E(X_1) - 2\sqrt{a}E(X_2) = 0$ ,

$$
E[\sqrt{b}(3X_3 - 4X_4)] = 3\sqrt{b}E(X_3) - 4\sqrt{b}E(X_4) = 0
$$

 ,

$$
D[\sqrt{a}(X_1 - 2X_2)] = aD(X_1) + 4aD(X_2) = 20a
$$

 ,

$$
D[\sqrt{b}(3X_3 - 4X_4)] = 9bD(X_3) + 16bD(X_4) = 100b
$$

 ,  
则 $\sqrt{a}(X_1 - 2X_2) \sim N(0, 20a)$ , $\sqrt{b}(3X_3 - 4X_4) \sim N(0, 100b)$ ,  
故当 $a = \frac{1}{20}$ , $b = \frac{1}{100}$ 时，

$$
\sqrt{a}(X_1 - 2X_2) \sim N(0, 1)
$$

 , $\sqrt{b}(3X_3 - 4X_4) \sim N(0, 1)$ ,  
又 $\sqrt{a}(X_1 - 2X_2)$ 与 $\sqrt{b}(3X_3 - 4X_4)$ 相互独立，所以 $Y \sim \chi^2(2)$ .  
小雀点睛：设 $X_1, X_2, \cdots, X_n$ 独立同分布于 $N(0, 1)$ , $Y = X_1^2 + X_2^2 + \cdots + X_n^2$ , 则 $Y \sim \chi^2(n)$ .

10. 解析  
    因为 $X_1 - X_2 \sim N(0, 2\sigma^2)$ , 所以 $\frac{X_1 - X_2}{\sqrt{2}\sigma} \sim N(0, 1)$ ,  
    又 $X_3 \sim N(0, \sigma^2)$ , 所以 $\frac{X_3}{\sigma} \sim N(0, 1)$ ,

$$
则 S = \frac{X_1 - X_2}{\sqrt{2}|X_3|} = \frac{X_1 - X_2}{\sqrt{2} \cdot \sqrt{X_3^2}} = \frac{\frac{X_1 - X_2}{\sqrt{2}\sigma} \cdot \sqrt{2}\sigma}{\sqrt{2} \cdot \sqrt{(\frac{X_3}{\sigma})^2 \cdot \sigma^2}} = \frac{\frac{X_1 - X_2}{\sqrt{2}\sigma}}{\sqrt{(\frac{X_3}{\sigma})^2}}
$$

 ,  
又 $\frac{X_1 - X_2}{\sqrt{2}\sigma}$ 与 $\frac{X_3}{\sigma}$ 相互独立，所以 $S \sim t(1)$ , 故本题选 (C) .  
小雀点睛：设 $X \sim N(0, 1)$ , $Y \sim \chi^2(n)$ , $X$ 与 $Y$ 相互独立， $Z = \frac{X}{\sqrt{Y/n}}$ , 则 $Z \sim t(n)$ .

11. 解析  
    对于 (A) , 因为 $X \not\sim N(0, 1)$ , 所以 $X^2 \not\sim \chi^2(1)$ , 则 (A) 错误；  
    对于 (B) , 因为 $X_i \sim N(0, \sigma^2)$ , 所以 $\frac{X_i}{\sigma} \sim N(0, 1)$ ,

## 第 354 页

又 $\frac{X_1}{\sigma}, \frac{X_2}{\sigma}, \cdots, \frac{X_{20}}{\sigma}$ 相互独立，

$$
则 \sum_{i=1}^{20} (\frac{X_i}{\sigma})^2 = \frac{20Y^2}{\sigma^2} \sim \chi^2(20)
$$

 , 所以 (B) 错误；  
对于 (C) , $\frac{X_{21}}{Y} = \frac{X_{21}}{\sqrt{\frac{1}{20}\sum_{i=1}^{20}X_i^2}} = \frac{\frac{X_{21}}{\sigma}}{\sqrt{\frac{1}{20}\sum_{i=1}^{20}(\frac{X_i}{\sigma})^2}} \sim t(20)$ , 所以 (C) 正确；  
对于 (D) , $\frac{X_{21}^2}{Y^2} = \frac{X_{21}^2}{\frac{1}{20}\sum_{i=1}^{20}X_i^2} = \frac{(\frac{X_{21}}{\sigma})^2}{\frac{1}{20}\sum_{i=1}^{20}(\frac{X_i}{\sigma})^2} \sim F(1, 20)$ , 所以 (D) 错误.  
故本题选 (C) .

12. 解析  
    由 $X \sim t(n)$ 可假设 $X = \frac{X_1}{\sqrt{\frac{X_2}{n}}}$ ,  
    其中 $X_1 \sim N(0, 1)$ , $X_2 \sim \chi^2(n)$ , 且 $X_1, X_2$ 相互独立，

$$
则 \frac{1}{X^2} = \frac{\frac{X_2}{n}}{X_1^2} = \frac{\frac{X_2}{n}}{\frac{X_1^2}{1}}
$$

 ,  
因为 $X_1^2 \sim \chi^2(1)$ , $X_2 \sim \chi^2(n)$ , 且 $X_1^2$ 与 $X_2$ 相互独立，  
所以 $\frac{1}{X^2} \sim F(n, 1)$ .  
小雀点睛：设 $X \sim \chi^2(m)$ , $Y \sim \chi^2(n)$ , $X$ 与 $Y$ 相互独立， $Z = \frac{X/m}{Y/n}$ , 则 $Z \sim F(m, n)$ .

13. 解析  
    因为 $X_i \sim N(0, 2^2)$ , 所以 $\frac{X_i}{2} \sim N(0, 1)$ ,

$$
Y = \frac{[(\frac{X_1}{2})^2 + (\frac{X_2}{2})^2 + \cdots + (\frac{X_9}{2})^2] \cdot 4}{[(\frac{X_{10}}{2})^2 + (\frac{X_{11}}{2})^2 + \cdots + (\frac{X_{15}}{2})^2] \cdot 8} = \frac{[(\frac{X_1}{2})^2 + (\frac{X_2}{2})^2 + \cdots + (\frac{X_9}{2})^2]}{2[(\frac{X_{10}}{2})^2 + (\frac{X_{11}}{2})^2 + \cdots + (\frac{X_{15}}{2})^2]}
$$

## 第 355 页

$$
= \frac{\frac{1}{10} \left[ (\frac{X_1}{2})^2 + (\frac{X_2}{2})^2 + \cdots + (\frac{X_{10}}{2})^2 \right]}{\frac{1}{5} \left[ (\frac{X_n}{2})^2 + (\frac{X_{12}}{2})^2 + \cdots + (\frac{X_{15}}{2})^2 \right]}
$$

又 $(\frac{X_1}{2})^2 + (\frac{X_2}{2})^2 + \cdots + (\frac{X_{10}}{2})^2$ 与 $(\frac{X_n}{2})^2 + (\frac{X_{12}}{2})^2 + \cdots + (\frac{X_{15}}{2})^2$ 相互独立，  
所以由F分布定义可知 $Y \sim F(10, 5)$。

14、解析

$$
Y = \frac{\frac{1}{2}(X_1^2 + X_2^2)}{\frac{1}{2}(X_3^2 + X_4^2)} \sim F(2, 2)
$$

又F分布的上 $\alpha$ 分位点满足 $F_{1-\alpha}(n_1, n_2) = \frac{1}{F_{\alpha}(n_2, n_1)}$，  
则 $F_{1-\alpha}(2, 2) = \frac{1}{F_{\alpha}(2, 2)}$，即 $Y_{1-\alpha} = \frac{1}{Y_{\alpha}}$，故本题选(A)。

15、解析  
对于(A)，因为 $X \sim N(0, 1)$，所以 $X^2 \sim \chi^2(1)$，则(A)错误；  
对于(B)，因为 $X \sim N(0, 1)$，所以 $\bar{X} \sim N(0, \frac{1}{n})$，则(B)错误；  
对于(C)，因为 $\frac{(n-1)S^2}{\sigma^2} \sim \chi^2(n-1)$，又 $\sigma^2 = 1$，  
所以 $(n-1)S^2 = (n-1) \cdot \frac{1}{n-1} \sum_{i=1}^n (X_i - \bar{X})^2 = \sum_{i=1}^n (X_i - \bar{X})^2 \sim \chi^2(n-1)$，  
则(C)正确；  
对于(D)，$S = \sqrt{\frac{1}{n-1} \sum_{i=1}^n (X_i - \bar{X})^2}$ 符合 $t(n-1)$ 分布的分母形式，  
但 $\bar{X} \not\sim N(0, 1)$，所以(D)错误，  
故本题选(C)。

16、解析  
因为 $X \sim N(0, \sigma^2)$，所以 $\bar{X} \sim N(0, \frac{\sigma^2}{n})$，标准化得 $\frac{\bar{X}}{\sigma/\sqrt{n}} \sim N(0, 1)$。

## 第 356 页

由 $\bar{X}$ 与 $S^2$ 独立可知 $\frac{\bar{X}}{\sigma/\sqrt{n}}$ 与 $\frac{(n-1)S^2}{\sigma^2}$ 独立，又 $\frac{(n-1)S^2}{\sigma^2} \sim \chi^2(n-1)$，  
则 $\frac{\sqrt{n}\bar{X}}{S} = \frac{\frac{\bar{X}}{\sigma/\sqrt{n}} \cdot \sigma}{\sqrt{\frac{1}{n-1}\frac{(n-1)S^2}{\sigma^2}} \cdot \sigma} = \frac{\frac{\bar{X}}{\sigma/\sqrt{n}}}{\sqrt{\frac{1}{n-1}\frac{(n-1)S^2}{\sigma^2}}} \sim t(n-1)$。

小雀点睛：正态总体下，关于 $S^2$ 与 $\bar{X}$ 有两个重要结论，① $S^2$ 与 $\bar{X}$ 相互独立；② $\frac{(n-1)S^2}{\sigma^2} \sim \chi^2(n-1)$。当然，这两个定理的证明过程远超本课程的要求，所以大家不必深究。

17、解析

$$
E(T) = E(\frac{1}{n}\sum_{i=1}^n X_i^2) = \frac{1}{n}\sum_{i=1}^n E(X_i^2)
$$

又 $E(X_i^2) = E(X^2) = D(X) + [E(X)]^2 = \sigma^2 + \mu^2$，  
故 $E(T) = \frac{1}{n}\sum_{i=1}^n E(X_i^2) = \frac{1}{n} \cdot n(\sigma^2 + \mu^2) = \sigma^2 + \mu^2$。

18、解析  
$E(\bar{X}) = E(X) = n$，$D(\bar{X}) = \frac{1}{n}D(X) = \frac{1}{n} \cdot 2n = 2$。  
小雀点睛：若 $X \sim \chi^2(n)$，则 $E(X)=n$，$D(X)=2n$。

19、解析  
因为 $X \sim B(1, p)$，所以 $E(X) = p$，$D(X) = p(1-p)$，  
则 $E(\bar{X}) = E(X) = p$，$D(\bar{X}) = \frac{1}{n}D(X) = \frac{p(1-p)}{n}$，$E(S^2) = D(X) = p(1-p)$。  
小雀点睛：若 $X_1, X_2, \cdots, X_n$ 是来自总体X的简单随机样本，且 $E(X) = \mu$，$D(X) = \sigma^2$，样本均值为 $\bar{X}$，样本方差为 $S^2$，则 $E(S^2) = \sigma^2$，证明如下（看懂即可）。  
将 $X_i - \bar{X}$ 拆为 $(X_i - \mu) - (\bar{X} - \mu)$，

## 第 357 页

则 $\sum_{i=1}^n (X_i - \bar{X})^2 = \sum_{i=1}^n [(X_i - \mu) - (\bar{X} - \mu)]^2$

$$
= \sum_{i=1}^n [(X_i - \mu)^2 - 2(\bar{X} - \mu)(X_i - \mu) + (\bar{X} - \mu)^2]
$$

$$
= \sum_{i=1}^n (X_i - \mu)^2 - 2(\bar{X} - \mu)\sum_{i=1}^n (X_i - \mu) + n \cdot (\bar{X} - \mu)^2
$$

又 $\sum_{i=1}^n (X_i - \mu) = \sum_{i=1}^n X_i - \sum_{i=1}^n \mu = n\bar{X} - n\mu = n(\bar{X} - \mu)$，  
所以 $\sum_{i=1}^n (X_i - \bar{X})^2 = \sum_{i=1}^n (X_i - \mu)^2 - 2(\bar{X} - \mu)\sum_{i=1}^n (X_i - \mu) + n \cdot (\bar{X} - \mu)^2$

$$
= \sum_{i=1}^n (X_i - \mu)^2 - 2(\bar{X} - \mu) \cdot n(\bar{X} - \mu) + n(\bar{X} - \mu)^2
$$

$$
= \sum_{i=1}^n (X_i - \mu)^2 - n(\bar{X} - \mu)^2
$$

则 $E(S^2) = E[\frac{1}{n-1}\sum_{i=1}^n (X_i - \bar{X})^2] = \frac{1}{n-1} \cdot E[\sum_{i=1}^n (X_i - \bar{X})^2]$

$$
= \frac{1}{n-1} \cdot E[\sum_{i=1}^n (X_i - \mu)^2 - n(\bar{X} - \mu)^2]
$$

$$
= \frac{1}{n-1} \cdot \sum_{i=1}^n E[(X_i - \mu)^2] - \frac{n}{n-1} E[(\bar{X} - \mu)^2]
$$

因为 $E(X_i) = \mu$，$E(\bar{X}) = \mu$，  
所以 $E[(X_i - \mu)^2] = D(X_i) = \sigma^2$，$E[(\bar{X} - \mu)^2] = D(\bar{X}) = \frac{\sigma^2}{n}$，  
故 $E(S^2) = \frac{1}{n-1} \cdot n\sigma^2 - \frac{n}{n-1} \cdot \frac{\sigma^2}{n} = \sigma^2$。

20、解析

$$
E(\bar{X} + S^2) = E(\bar{X}) + E(S^2) = \mu + \sigma^2
$$

$$
D(\bar{X} + S^2) = D(\bar{X}) + D(S^2)
$$

又 $D(\bar{X}) = \frac{\sigma^2}{n}$，

$$
D(S^2) = D\left[\frac{(n-1)S^2}{\sigma^2} \cdot \frac{\sigma^2}{n-1}\right] = \frac{\sigma^4}{(n-1)^2} \cdot D\left[\frac{(n-1)S^2}{\sigma^2}\right]
$$

$$
= \frac{\sigma^4}{(n-1)^2} \cdot 2(n-1) = \frac{2\sigma^4}{n-1}
$$

所以 $D(\bar{X} + S^2) = \frac{\sigma^2}{n} + \frac{2\sigma^4}{n-1}$。

## 第 358 页

小崔点睛：本题运用了下列结论．  
① 正态总体下，$S^2$与$\bar{X}$相互独立；  
② 正态总体下，

$$
\frac{(n-1)S^2}{\sigma^2} \sim \chi^2(n-1)
$$

；  
③ 若 $X \sim \chi^2(n)$，则 $E(X) = n$，$D(X) = 2n$．
