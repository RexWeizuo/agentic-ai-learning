---
title: 习题
date: 2026-05-22T10:13:09+08:00
lastmod: 2026-05-22T10:13:09+08:00
---

# 习题

## 第 345 页

第六章 数理统计的基本概念

1. 设 $X_1, X_2, \dots, X_n$ 是来自总体 $X \sim N(\mu, \sigma^2)$ 的简单随机样本，  
   其中 $\mu$ 已知，$\sigma^2$ 未知，则下列不是统计量的是 ( ).  
   (A) $\frac{1}{n}\sum_{i=1}^n X_i$  
   (B) $\sum_{i=1}^n (\frac{X_i - \mu}{\sigma})^2$  
   (C) $\frac{1}{n}\sum_{i=1}^n (X_i - \mu)$  
   (D) $\min_{1 \leqslant i \leqslant n} \{X_i\}$
2. 设 $X_1, X_2, \dots, X_n$ 是来自总体 $X \sim N(2, 4^2)$ 的简单随机样本，  
   样本均值 $\bar{X} = \frac{1}{n}\sum_{i=1}^n X_i$，则 ( ).  
   (A) $\frac{\bar{X} - 2}{4} \sim N(0, 1)$  
   (B) $\frac{\bar{X} - 2}{1} \sim N(0, 1)$  
   (C) $\frac{\bar{X} - 2}{\frac{4}{\sqrt{n}}} \sim N(0, 1)$  
   (D) $\frac{\bar{X} - 2}{2} \sim N(0, 1)$
3. 设 $X_1, X_2, \dots, X_n$ 是来自总体 $X \sim N(\mu, \sigma^2)$ 的简单随机样本，  
   样本均值 $\bar{X} = \frac{1}{n}\sum_{i=1}^n X_i$，则 $P\{|\bar{X} - \mu| < \sigma\}$ 的值 ( ).  
   (A) 与 $\sigma$ 相关  
   (B) 与 $\mu$ 相关  
   (C) 与 $n$ 相关  
   (D) 为常数
4. 设随机变量 $X, Y$ 独立同分布于 $N(\mu, \sigma^2)$，$\bar{X}$ 和 $\bar{Y}$ 分别为来自总体  
   $X$ 和 $Y$ 的容量为 $n$ 的简单随机样本的样本均值，则当 $n$ 固定时，  
   $P\{|\bar{X} - \bar{Y}| > \sigma\}$ 的值随着 $\sigma$ 的增大而 ( ).  
   (A) 单调增加  
   (B) 单调减小  
   (C) 保持不变  
   (D) 无法确定
5. 设 $X_1, X_2, \dots, X_n$ 是来自总体 $X \sim N(\mu, \sigma^2)$ 的简单随机样本，  
   $n \geqslant 2$，样本均值 $\bar{X} = \frac{1}{n}\sum_{i=1}^n X_i$，令 $P_1 = P\{|X - \mu| \leqslant \varepsilon\}, P_2 = P\{|\bar{X} - \mu| \leqslant \varepsilon\},$

## 第 346 页

则对任意 $\varepsilon > 0$, $P_1$ 与 $P_2$ 的大小关系为 ( ).  
(A) $P_1 > P_2$  
(B) $P_1 < P_2$  
(C) $P_1 = P_2$  
(D) 无法确定

6. 设随机变量 $X \sim N(0, 1)$, 对给定的 $\alpha \in (0, 1)$, $u_\alpha$ 满足 $P\{X \geqslant u_\alpha\} = \alpha$, 若 $P\{|X| < x\} = \alpha$, 则 $x = ( )$.  
   (A) $U_{\frac{\alpha}{2}}$  
   (B) $U_{1-\frac{\alpha}{2}}$  
   (C) $U_{\frac{1-\alpha}{2}}$  
   (D) $U_{1-\alpha}$
7. 设 $X_1, X_2, \cdots, X_n, X_{n+1}$ 是来自总体 $X \sim N(\mu, \sigma^2)$ 的简单随机样本, $\bar{X} = \frac{1}{n}\sum_{i=1}^n X_i$.  
   (1) 求 $X_{n+1} - \bar{X}$ 服从的分布;  
   (2) 求 $X_1 - \bar{X}$ 服从的分布.
8. 设 $X_1, X_2, \cdots, X_6$ 是来自总体 $X \sim N(0, 1)$ 的简单随机样本, $Y = (X_1 + X_2 + X_3)^2 + (X_4 + X_5 + X_6)^2$, 若 $kY$ 服从 $\chi^2$ 分布, 求常数 $k$ 的值.
9. 设 $X_1, X_2, X_3, X_4$ 是来自总体 $X \sim N(0, 2^2)$ 的简单随机样本, $Y = a(X_1 - 2X_2)^2 + b(3X_3 - 4X_4)^2, a > 0, b > 0$, 试确定常数 $a, b$ 的值, 使 $Y$ 服从 $\chi^2$ 分布.
10. 设 $X_1, X_2, X_3$ 是来自总体 $X \sim N(0, \sigma^2)$ 的简单随机样本, 则统计量 $S = \frac{X_1 - X_2}{\sqrt{2}|X_3|}$ 服从的分布为 ( ).  
    (A) $F(1, 1)$  
    (B) $F(2, 1)$  
    (C) $t(1)$  
    (D) $t(2)$

## 第 347 页

11. 设 $X_1, X_2, \cdots, X_{21}$ 是来自总体 $X \sim N(0, \sigma^2)$ 的简单随机样本, $Y^2 = \frac{1}{20}\sum_{i=1}^{20} X_i^2$, 则下列结论正确的是 ( ).  
    (A) $X^2 \sim \chi^2(1)$  
    (B) $Y^2 \sim \chi^2(20)$  
    (C) $\frac{X_{21}}{Y} \sim t(20)$  
    (D) $\frac{X_{21}^2}{Y^2} \sim F(20, 1)$
12. 设随机变量 $X$ 服从 $t(n)$ 分布, 则 $\frac{1}{X^2}$ 服从的分布为 \_\_\_\_\_\_.
13. 设 $X_1, X_2, \cdots, X_{15}$ 是来自总体 $X \sim N(0, 2^2)$ 的简单随机样本, 则随机变量 $Y = \frac{X_1^2 + X_2^2 + \cdots + X_{10}^2}{2(X_{11}^2 + X_{12}^2 + \cdots + X_{15}^2)}$ 服从的分布为 \_\_\_\_\_\_.
14. 设 $X_1, X_2, X_3, X_4$ 是来自总体 $X \sim N(0, 1)$ 的简单随机样本, $Y = \frac{X_1^2 + X_2^2}{X_3^2 + X_4^2}$, 对给定的 $\alpha \in (0, 1)$, $y_\alpha$ 满足 $P\{Y > y_\alpha\} = \alpha$, 则 ( ).  
    (A) $y_\alpha \cdot y_{1-\alpha} = 1$  
    (B) $y_\alpha \cdot y_{1-\frac{\alpha}{2}} = 1$  
    (C) $y_{\frac{\alpha}{2}} \cdot y_{1-\frac{\alpha}{2}} = 1$  
    (D) $y_{\frac{\alpha}{2}} \cdot y_{1-\alpha} = 1$
15. 设 $X_1, X_2, \cdots, X_n$ 是来自总体 $X \sim N(0, 1)$ 的简单随机样本, 样本均值 $\bar{X} = \frac{1}{n}\sum_{i=1}^n X_i$, 样本方差 $S^2 = \frac{1}{n-1}\sum_{i=1}^n (X_i - \bar{X})^2$, 则 ( ).  
    (A) $X^2 \sim t(n-1)$  
    (B) $\bar{X} \sim N(0, 1)$  
    (C) $\sum_{i=1}^n (X_i - \bar{X})^2 \sim \chi^2(n-1)$  
    (D) $\frac{\bar{X}}{S} \sim t(n-1)$
16. 设 $X_1, X_2, \cdots, X_n$ 是来自总体 $X \sim N(0, \sigma^2)$ 的简单随机样本, 样本均值 $\bar{X} = \frac{1}{n}\sum_{i=1}^n X_i$, 样本方差 $S^2 = \frac{1}{n-1}\sum_{i=1}^n (X_i - \bar{X})^2$, 则统计量 $\frac{\sqrt{n}\bar{X}}{S}$ 服从的分布为 \_\_\_\_\_\_.

## 第 348 页

17. 设 $X_1, X_2, \cdots, X_n$ 是来自总体 $X \sim N(\mu, \sigma^2)$ 的简单随机样本, 记 $T = \frac{1}{n}\sum_{i=1}^n X_i^2$, 则 $T$ 的期望 $E(T) =$ \_\_\_\_\_\_.
18. 设 $X_1, X_2, \cdots, X_n$ 是来自总体 $X \sim \chi^2(n)$ 的简单随机样本, 样本均值 $\bar{X} = \frac{1}{n}\sum_{i=1}^n X_i$, 则 $E(\bar{X}) =$ \_\_\_\_\_\_, $D(\bar{X}) =$ \_\_\_\_\_\_.
19. 设 $X_1, X_2, \cdots, X_n$ 是来自二项总体 $X \sim B(1, p)$ 的简单随机样本, 样本均值 $\bar{X} = \frac{1}{n}\sum_{i=1}^n X_i$, 样本方差 $S^2 = \frac{1}{n-1}\sum_{i=1}^n (X_i - \bar{X})^2$, 则 $E(\bar{X}) =$ \_\_\_\_\_\_, $D(\bar{X}) =$ \_\_\_\_\_\_, $E(S^2) =$ \_\_\_\_\_\_.
20. 设 $X_1, X_2, \cdots, X_n$ 是来自总体 $X \sim N(\mu, \sigma^2)$ 的简单随机样本, 样本均值 $\bar{X} = \frac{1}{n}\sum_{i=1}^n X_i$, 样本方差 $S^2 = \frac{1}{n-1}\sum_{i=1}^n (X_i - \bar{X})^2$, 则 $E(\bar{X} + S^2) =$ \_\_\_\_\_\_, $D(\bar{X} + S^2) =$ \_\_\_\_\_\_.
