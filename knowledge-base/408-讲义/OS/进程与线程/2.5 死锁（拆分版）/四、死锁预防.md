---
title: 四、死锁预防
date: 2026-05-17T00:00:00+08:00
lastmod: 2026-05-21T09:50:12+08:00
---

# 四、死锁预防

```
``````

# 四、死锁预防

（一）死锁预防概述

死锁预防是一种静态的死锁解决方法。在产生死锁的四个必要条件均满足时，系统才可能进入死锁状态，因此可以通过破坏四个必要条件中的一个或多个来防止死锁的发生。

## （一）破坏互斥条件

### 🧠 深度理解：SPOOLing 如何破坏互斥条件（来自 2.5 DS）

> **Q：**  多个进程共享临界资源必须保证互斥访问，反过来说如果只有一个进程访问资源就没必要保证互斥访问。SPOOLing 将打印机变成逻辑共享设备是怎么做到破坏互斥的？

> **A：**  安排一个唯一的"打印管理进程"当代理，其他进程不直接碰打印机，只把数据写到磁盘缓冲区。真正访问打印机的只有那个代理，只有一个进程在用，互斥条件形同虚设。这是通过**绕开互斥**来预防死锁——不是去掉互斥，而是让互斥场景根本不存在。

> **Q：**  说白了就是加了一个代理协调，代理给打印机请求排队是吗？

> **A：**  对。不加代理时多进程直接抢打印机→拿到就用、拿不到就阻塞。加代理后进程只管把打印任务塞进磁盘队列，打印管理进程按顺序逐个打印。变成了"排队叫号"模式，进程提交任务后继续干别的事，不阻塞。

多个进程共享临界资源必须保证互斥访问，反过来说，如果只有一个进程访问临界资源就没必要保证互斥访问。对于某些资源，可以为其建立一个专门的进程，用来代理其他进程对这种资源的访问。其他进程想要访问这种资源，就必须告知代理进程。访问这类资源的进程只有代理进程一个，自然不会产生死锁。例如，SPOOLing 技术就能将打印机改为逻辑上的共享设备，达到死锁预防的效果。

[图片：P1、P2请求打印机，以及通过代理进程请求逻辑打印机的流程图]

这一方式的缺点在于条件严苛，只有少数资源（如打印机）适合这一方式。因此，预防死锁主要是破坏产生死锁的后三个条件。

## （二）破坏请求和保持条件

### 🧠 深度理解：第二种协议如何破坏请求与保持条件（来自 2.5 DS）

> **Q：**  第二种协议中"进程只须请求磁带机、磁盘即可运行，数据复制完释放掉再去请求打印机"——复制数据不还需要磁带机吗，怎么做到提前释放的？

> **A：**  请求磁带机拿到控制权后，CPU 把磁带上的数据搬到磁盘，这个"复制"用的是 **CPU** 而不是磁带机（磁带机只负责输出数据流）。复制完成后磁带机就空闲了，可以释放它再去请求打印机。关键是把"持有一资源等另一资源"的长周期拆成了短周期操作，减少死锁窗口。

> **Q：**  所以进程并不是在等，而是在执行 CPU 的复制逻辑？

> **A：**  对。请求资源只是拿控制权（你拿到车的钥匙），使用资源才是真正干活（你开车上路）。拿到磁带机控制权后 CPU 执行复制指令，进程确实在用 CPU 干活，不是空等。只有请求不到资源时才被阻塞等待。

> **Q：**  请求阶段可能等（磁盘不空闲），数据复制时进程也在等（等 I/O 完成），两个阶段不都是在等不是在干活吗？

> **A：**  两个"等"本质不同：请求阶段等的是**资源被别人占用**——你手里没货，不释放你就进不去，这叫阻塞等待；复制阶段等的是 **I/O 设备完成传输**——你手里已经拿着资源了，只是活还没干完，这叫忙等。会导致死锁的是"攥着资源等别人手里的资源"，第二种协议让你先放手再等，避免"手里攥着不放"的局面。

为了能够破坏“请求和保持”条件，系统必须保证做到：当一个进程在请求资源时，它不能持有不可抢占资源。该保证可通过以下两个不同的协议实现。

### （1）第一种协议（开始之前就申请好全部资源）

该协议规定，所有进程在开始运行之前，必须一次性地申请其在整个运行过程中所需的全部资源。这样，该进程在整个运行期间便不会再提出资源要求，从而破坏了“请求”条件。

系统在分配资源时，只要有一种资源不能满足进程的要求，则即使所需的其他资源都空闲也不分配给该进程，而是让其等待。由于该进程在等待期间未占有任何资源，从而破坏了“保持”条件。

[图片：硬盘和打印机]

第一种协议的优点是简单、易行、安全。但缺点也极其明显。

① 资源被严重浪费，严重地降低了资源利用率。进程在开始运行时，就会一次性地占用整个运行过程所需的全部资源，其中有些资源可能仅在运行初期或运行快结束时才会使用。

② 进程经常会发生饥饿现象。因为仅当进程在获得其所需的全部资源后才能开始运行，所以可能由于个别资源长期被其他进程占用，等待该资源的进程迟迟不能开始运行。

### （2）第二种协议（只申请初期需要资源，边运行边释放）

该协议是对第一种协议的改进，它允许一个进程只获得运行初期所需的资源后，便开始运行。进程运行过程中再逐步释放已分配给自己的且已用毕的全部资源，然后再请求新的所需资源。

下面通过一个具体例子来说明第二种协议比第一种协议要好。

【例】例如有一个进程，它所要完成的任务是，先将数据从磁带上复制到磁盘上，然后对数据进行处理，最后把结果打印出来。

[图片：数据从磁带复制到磁盘，再复制到打印机]

攥着资源等 vs 空手等

① 在采用第一种协议时，进程必须在开始时就请求磁带机、磁盘和打印机，然而打印机仅在最后才会用到，所以刚开始就请求占用打印机，既影响其利用率，还会影响其他进程的运行。此外，若刚开始时磁带机和磁盘空闲，但因打印机被很多进程所需而分配给了其他进程，则该进程还需要等待。

② 在采用第二种协议时，进程在开始时只须请求磁带机、磁盘，然后即可运行。等到磁带上的全部数据已复制到磁盘中并已处理好后，便可将磁带机和磁盘释放掉，再去请求打印机。这不仅能使进程更快地完成任务，提高设备的利用率，还可减少进程发生饥饿现象的概率。

## （三）破坏不可抢占条件（资源申请失败，就释放已保持的左右资源）

为了能破坏“不可抢占”条件，协议中规定，当一个已经保持了某些不可抢占资源的进程提出新的资源请求而不能得到满足时，它必须释放已经保持的所有资源，待以后需要时再重新申请。这意味着进程已占有的资源会被暂时释放，从而破坏了“不可抢占”条件。

该方法实现起来比较复杂，且需要付出很大的代价。因为一个不可抢占的资源（如打印机、刻录机等）在使用一段时间后被抢占，可能会造成进程前一阶段工作的失效。

[图片：光盘/刻录机]

## （四）破坏循环等待条件

一个能保证“循环等待”条件不成立的方法是，对系统的所有资源进行编号，并要求进程只能按资源编号严格递增或递减的顺序请求资源。

例如，进程在申请资源时，如果一个资源的编号小于进程已获得的某个资源的编号，则进程不能请求该资源。

【例】假设在系统中有 10 个资源，可以把它们编号为 R1,R2,...,R10。假设进程 P1 当前已经申请了资源 R1、R3 和 R6。此后，它就不能再申请 R6 之前的资源了，只能申请 R6 以后的资源。

[图片：资源编号 R1 到 R10，箭头指向 P0]

可以证明，该方案下，死锁无法发生。

【例】假设 P0、P1、P2、P3 分别占用 R0、R1、R2、R3，P0、P1、P2 依次申请 R1、R2、R3。

[图片：资源分配图，显示进程P0、P1、P2、P3与资源R0、R1、R2、R3之间的分配与请求关系]

若要实现循环等待链，则 P3 必须申请 P0 占用的 R0，显然，由于 R0 编号小于 R3，因此 P3 不可能有申请 R0 的请求，如果其需要 R0，则必须在申请 R3 前就申请 R0。因此，无法形成循环等待链。

这种预防死锁策略和前两种策略相比，其资源利用率和系统吞吐量都有比较明显的改善，但也存在下列问题（了解即可）。

① 为系统中各类资源规定的序号必须相对稳定，这限制了新类型设备的增加。

② 尽管在为资源的类型分配序号时已经考虑到了大多数作业在实际使用这些资源时的顺序，但也经常会发生这种情况：作业使用各类资源的顺序与系统规定的顺序不同，造成对资源的浪费。

第三，为了方便用户，系统对用户在编程时所施加的限制条件应尽量少，然而这种按规定次序申请资源的方法必然会限制用户进行简单、自主的编程。

【练习】一次分配所有资源的方法可以预防死锁的发生，它破坏死锁 4 个必要条件中的（ ）。

A.互斥

B.占有并请求

C.非剥夺

D.循环等待

答案：B 严格来说是请求

## （五）哲学家进餐问题

如图所示，有五个哲学家，他们共用一张圆桌，围坐在五把椅子上，桌上有五个碗和五只筷子，每根筷子只能互斥使用。

[图片：哲学家进餐示意图，五只猫代表哲学家围坐在圆桌旁，桌上有碗和筷子]

当哲学家饥饿的时候，他会尝试拿自己左右两边的筷子，只有当他手上有两根筷子的时候才能吃饭。饭后，他放下筷子，继续思考。为了实现对筷子的互斥使用，可以用 1 个信号量表示 1 根筷子，由这 5 根筷子对应的 5 个信号量构成信号量数组，描述如下。

```c

semaphore chopstick[5]={1,1,1,1,1}; //初始化信号量

//其中第i位哲学家的活动可描述如下：

while (TRUE) {

    P(chopstick[i]);          //取左边筷子

    P(chopstick[(i+1) % 5]);  //取右边筷子

    ...

    吃饭;

    ...

    V(chopstick[i]);          //放下左边筷子

    V(chopstick[(i+1) % 5]);  //放下右边筷子

    ...

    思考:

    ...

}

```

在上述描述中，哲学家饥饿时总是会先去拿他左边的筷子，即执行 wait(chopstick[i])；成功后，再去拿他右边的筷子，即执行 wait(chopstick [(i+1)%5])；又成功后便可进餐。进餐完毕，先放下他左边的筷子，再放下他右边的筷子。

上述解法有可能引起死锁。假如 5 位哲学家同时饥饿而各自拿起左边的筷子，这时就会使 5 个信号量 chopstick 均为 0；当他们再试图去拿右边的筷子时，都将会因无筷子可拿而进行无限期的等待。对于这样的死锁问题，可采取以下 3 种方法进行解决。

（1）至多只允许有 4 位哲学家同时去拿左边的筷子，最终能保证至少有 1 位哲学家能够进餐，并在进餐毕时能释放出他用过的 2 根筷子，从而使更多的哲学家能够进餐。

（2）仅当哲学家的左右两根筷子均可用时，才允许他拿起筷子进餐。

（3）规定奇数号哲学家先拿他左边的筷子，然后再去拿他右边的筷子；而偶数号哲学家则相反。按此规定，5 位哲学家都先竞争奇数号筷子，获得后，再去竞争偶数号筷子，最后总会有一位哲学家能获得两根筷子而进餐。

基于方法（1）的代码如下。

```c

semaphore chopstick[5]={1,1,1,1,1}; //初始化信号量

semaphore priority=4;               //设置允许拿筷子的哲学家数量

//其中第 i 位哲学家的活动可描述如下：

while (TRUE) {

    P(priority);

    P(chopstick[i]);                //取左边筷子

    P(chopstick[(i+1) % 5]);        //取右边筷子

    ...

    吃饭;

    ...

    V(chopstick[i]);                //放下左边筷子

    V(chopstick[(i+1) % 5]);        //放下右边筷子

    V(priority);

    ...

    思考:

    ...

}

```

【练习】在哲学家就餐问题中，若同时存在左撇子和右撇子（将先拿起左边筷子的人称为左撇子，而将先拿起右边筷子的人称为右撇子），则不会发生死锁，因为破坏了（ ）。

A.互斥条件        B.请求与保持条件        C.不剥夺条件        D.循环等待条件

答案：D

### 🧠 深度理解：哲学家进餐与破坏循环等待（来自 2.5 DS）

> **Q：**  哲学家进餐用 `semaphore priority = 4` 限制，这个解法的逻辑是什么？

> **A：**  最多 4 人同时拿筷子，必然至少有 1 人能拿到两只筷子吃上饭。吃完释放筷子唤醒下一个。本质是破坏循环等待条件——限制并发数后，不可能出现"每个人都只拿了一只筷子"的循环等待局面。

> **Q：**  【练习】左撇子右撇子混合不会发生死锁，因为破坏了（ ）？

> **A：**  选 **C 破坏循环等待条件**。全左撇子（都先拿左边）时形成单向循环等待环。混合左右撇子后必然有人拿筷子顺序和别人相反，这个人打断了循环等待链——环被打破。

‍

👤 **User**

根据文档出一些题目检测我

---

🤖 **Assistant**

根据提供的文档内容，我为你设计了以下题目，用于检测对死锁基本概念的理解：

---

### 一、单选题

1. 死锁发生时，至少涉及几个进程？  
   A. 1个  
   B. 2个  
   C. 3个  
   D. 不一定
2. 以下哪种现象是由于资源分配不合理造成的？  
   A. 死循环  
   B. 死锁  
   C. 程序逻辑错误  
   D. 数组越界
3. 一个进程长期处于就绪态，但一直得不到 CPU，这属于：  
   A. 死锁  
   B. 饥饿  
   C. 死循环  
   D. 正常现象
4. 关于死锁、饥饿、死循环，下列说法正确的是：  
   A. 死循环是因为资源分配不合理  
   B. 饥饿是程序的设计错误引起  
   C. 死锁是所有进程都想得到资源但又都得不到资源的状态  
   D. 饥饿和死锁都会导致进程永远被阻塞且无法恢复

---

### 二、判断题

5. 死锁中的每个进程都不愿意释放自己已获得的资源，必须等待其他进程释放资源后自己才可能继续运行。 （ ）
6. 进程因为等待 I/O 设备而长期阻塞，这种情况一定是死锁。 （ ）

---

### 三、简答题

7. 请用自己的话解释死锁的定义，并说明死锁、饥饿、死循环三者的主要区别是什么？
8. 文档中用两只猫吃饭的图片形象地比喻了死锁，请简要描述这幅图是如何体现死锁特征的。

---

### 四、开放题

9. 如果让你设计一个简单的资源分配系统，你会在哪些环节特别留意以避免死锁的发生？请结合死锁的定义谈谈你的想法。

---

**参考答案提示**  
1.B  2.B  3.B  4.C  5.√  6.×  7. 死锁是多个进程循环等待彼此拥有的资源，均无法推进；饥饿是单个进程长期得不到资源；死循环是程序自身逻辑错误。死锁和饥饿与资源分配有关，死循环与程序设计有关。 8. 两只猫各自占有一部分资源（筷子或碗），又都需要对方占有的资源才能吃饭，并且都不放弃已占资源，形成循环等待，即死锁。 9. 要点可包括：避免循环等待、限制资源持有等待、统一资源分配顺序等。

---

👤 **User**

参考答案要详解

---

🤖 **Assistant**

好的，以下是对每题参考答案的详细解析：

---

### 一、单选题

**1. 死锁发生时，至少涉及几个进程？**   
**答案：B. 2个**  
**详解：**   
根据文档定义：“死锁指一组并发进程中的每一个进程都等待其余进程中的某个进程拥有的资源”，并且“死锁是 n 个进程 (n≥2) 循环等待对方手里的资源”。死锁的本质是多个进程之间形成资源等待的闭环，最少需要两个进程互相等待对方持有的资源，因此 n 至少为 2。只涉及一个进程的情况不可能形成循环等待，不属于死锁（可能是饥饿或死循环）。

---

**2. 以下哪种现象是由于资源分配不合理造成的？**   
**答案：B. 死锁**  
**详解：**   
文档明确指出：死锁和饥饿是由于资源的不合理分配引起，而死循环是由于程序的设计错误引起。选项中的 A（死循环）和 C（程序逻辑错误）、D（数组越界）均属于程序的逻辑/编码错误，只有死锁属于资源分配不合理导致的现象。饥饿也同样由资源分配不合理引起，但选项中未出现。

---

**3. 一个进程长期处于就绪态，但一直得不到 CPU，这属于：**   
**答案：B. 饥饿**  
**详解：**   
文档解释：饥饿是进程由于长期未获得想要的资源，从而无法向前推进的现象。资源可以是 CPU —— 饥饿进程长期处于就绪态（等待 CPU 调度）。而死锁要求多个进程互相等待且都被阻塞，不会只是单个进程处于就绪态；死循环是程序一直在循环体内执行，通常占用 CPU 并不处于就绪等待。

---

**4. 关于死锁、饥饿、死循环，下列说法正确的是：**   
**答案：C. 死锁是所有进程都想得到资源但又都得不到资源的状态**  
**详解：**

- A 错误：死循环是程序设计错误引起，不是资源分配不合理。
- B 错误：饥饿是资源分配不合理引起，不是程序设计错误。
- C 正确：文档定义“从而造成所有并发进程都想得到资源而又都得不到资源，均不能继续向前推进的状态”正是对死锁的描述。
- D 错误：饥饿不一定永久，例如低优先级进程长期得不到 CPU，但一旦高优先级结束它仍可能运行；而文档并未断言饥饿一定无法恢复。死锁中的进程如果外界不干预可能永远阻塞，但该选项说法太绝对且混为一谈。

---

### 二、判断题

**5. 死锁中的每个进程都不愿意释放自己已获得的资源，必须等待其他进程释放资源后自己才可能继续运行。 （√）**   
**详解：**   
文档指出：“这些并发进程在得到自己需要的资源之前不会释放自己拥有的资源”，这导致进程都占有部分资源并等待其它资源，人为无法推进。因此进程确实不会主动释放资源，必须等待其他进程释放，但死锁状态下谁都无法释放，所以大家都无法继续运行。陈述符合定义。

---

**6. 进程因为等待 I/O 设备而长期阻塞，这种情况一定是死锁。 （×）**   
**详解：**   
文档说明：饥饿的“资源”可以是 I/O 设备，饥饿进程长期处于阻塞态。单个进程等待 I/O 设备长期得不到响应，可能是由于资源分配不合理造成的饥饿，但不构成死锁。死锁必须涉及多个进程之间循环等待彼此的资源。因此不一定是死锁。

---

### 三、简答题

**7. 请用自己的话解释死锁的定义，并说明死锁、饥饿、死循环三者的主要区别是什么？**   
**参考答案要点：**   
死锁定义：多个进程在执行过程中，各自持有一些资源，并互相等待对方持有的资源释放，且每个进程在获得所需全部资源前都不会释放已占资源，导致所有涉及的进程都被阻塞，无法继续执行。

主要区别：

- **死锁**：发生在两个或以上进程之间，循环等待对方占有的资源，所有进程都被阻塞。
- **饥饿**：单个进程长期得不到所需资源（如 CPU、I/O 等），可能由于调度策略或资源分配不公平造成，进程本身可能仍处于就绪或阻塞，但不涉及循环等待。
- **死循环**：程序内部逻辑错误导致一直在某段代码循环执行，不涉及资源争夺或多个进程等待，该进程可能一直占用 CPU。
- 根源不同：死锁和饥饿通常由资源分配不当造成，死循环源自程序设计错误。

---

**8. 文档中用两只猫吃饭的图片形象地比喻了死锁，请简要描述这幅图是如何体现死锁特征的。**   
**参考答案要点：**   
图中每只猫都占用了一部分“吃饭”所需要的资源：比如一只猫拿着筷子，另一只猫拿着碗。要吃饭必须同时拥有筷子和碗。每只猫都在等待对方释放手中的资源，但都不会主动放弃自己已有的资源，于是两只猫都无法开始吃饭，陷入僵持状态。这形象地表现了死锁的四个关键要素：互斥（资源一次只能被一只猫使用）、持有并等待（占有一部分资源并等待其他）、不剥夺（猫不会强行抢对方资源）、循环等待（猫A等猫B释放碗，猫B等猫A释放筷子），完全符合死锁特征。

---

### 四、开放题

**9. 如果让你设计一个简单的资源分配系统，你会在哪些环节特别留意以避免死锁的发生？请结合死锁的定义谈谈你的想法。**   
**参考思路：**   
结合死锁产生的必要条件（互斥、持有等待、不可剥夺、循环等待），可以从以下几个方面入手：

- **破坏“持有且等待”** ：要求进程在开始执行前一次性申请所有所需资源，如果申请不到则等待，但不持有部分资源。或者当新资源申请不到时，主动释放已占资源，再重新尝试。
- **破坏“不可剥夺”** ：允许系统抢占某些资源，例如设置优先级机制，高优先级进程可以剥夺低优先级进程持有的资源，但需妥善处理被剥夺进程的回退。
- **破坏“循环等待”** ：对资源进行编号排序，规定所有进程必须按照递增顺序申请资源，从而避免循环等待环的形成。
- **死锁检测与恢复**：如果无法完全预防，可定时检测资源分配图是否存在环，若有则通过终止进程或回滚操作来解除死锁。
- **避免策略**：利用银行家算法等，在分配前判断是否安全状态，仅在分配后系统仍处于安全状态时才分配资源。  
  （学生答案应清晰体现至少一种具体措施，并能联系死锁定义解释为何能避免进程永远阻塞等待。）

---

👤 **User**

“死锁要求多个进程互相等待且都被阻塞，不会只是单个进程处于就绪态“ 死锁多个进程都处于阻塞态吗

---

🤖 **Assistant**

是的，根据文档的定义，**死锁中的多个进程都处于阻塞态**。

文档原文：“死锁是 n 个进程 (n≥2) 循环等待对方手里的资源，**导致 n 个进程都被阻塞**，均无法向前推进的现象。”

这明确指出了死锁状态下的所有涉及进程全部被阻塞，而不会是就绪态或其他状态。

因此，在第3题解析中说“死锁要求多个进程互相等待且都被阻塞，不会只是单个进程处于就绪态”是正确的——单个进程长期就绪但得不到 CPU 属于饥饿（可能处于就绪态），而死锁是多个进程阻塞等待彼此的资源。

---

👤 **User**

出一些题和答案详解

---

🤖 **Assistant**

根据文档《二、死锁产生的原因》，我为你设计了以下题目，并附上答案详解。

---

### 一、单选题

**1. 根据文档，死锁的起因通常不包括以下哪个要素？**   
A. 竞争不可抢占资源  
B. 竞争可消耗资源  
C. 进程推进顺序不当  
D. 进程优先级错误

**答案：D**  
**详解：**   
文档明确列出死锁的起因包含三个要素：“分别是对不可抢占资源进行争夺、对可消耗资源进行争夺以及进程推进顺序不当”。优先级错误虽然可能导致饥饿，但并非文档指出的死锁起因三要素之一。

---

**2. 以下哪个设备属于不可抢占资源？**   
A. CPU  
B. 内存  
C. 打印机  
D. 高速缓存

**答案：C**  
**详解：**   
文档举例：刻录机、磁带机、打印机等都属于不可抢占资源。CPU 和内存可被操作系统剥夺和重新分配，属于可抢占资源（或至少可被重新调度和换出）。打印机在被一个进程占用期间如果强行分配给另一个进程会造成输出混乱，因此属于不可抢占资源。

---

**3. 进程 P1 和 P2 按以下顺序执行时，哪个顺序会导致死锁？**   
代码逻辑：  
P1 先打开 F1，后打开 F2  
P2 先打开 F2，后打开 F1  
A. P1 打开 F1 和 F2 完成后，P2 才开始打开 F2  
B. P1 打开 F1 的同时，P2 打开 F2，然后两者继续请求另一个文件  
C. P2 打开 F2 和 F1 完成后，P1 才开始打开 F1  
D. P1 放弃打开 F2，只打开 F1

**答案：B**  
**详解：**   
文档分析：若 P1 和 P2 顺序执行（A、C 情况），由于先执行的进程释放资源后另一个再执行，不会死锁。若同时 P1 占有 F1、P2 占有 F2，然后 P1 请求 F2、P2 请求 F1，则形成循环等待，两者均阻塞，导致死锁。D 情况放弃资源也不符合死锁的“持有并等待”必要条件。

---

**4. 关于可消耗资源，以下描述正确的是：**   
A. 可消耗资源是永久存在的设备资源  
B. 消息是可消耗资源的一个典型例子  
C. 打印机属于可消耗资源  
D. 可消耗资源不能被动态创建

**答案：B**  
**详解：**   
文档定义：“可消耗资源指进程运行期间动态创建和消耗的资源，通常是由生产者进程创建、由消费者进程消耗的，最典型的可消耗资源就是用于进程间通信的消息。” 打印机是不可抢占资源（设备），属于持久资源；可消耗资源可以动态创建和消耗。

---

**5. 三个进程 P1、P2、P3 通过消息通信，以下哪种执行顺序会发生死锁？**   
A. 每个进程先 send 后 receive  
B. 每个进程先 receive 后 send  
C. P1 先 send，P2 先 receive，P3 先 send  
D. 交替执行，无固定顺序

**答案：B**  
**详解：**   
文档给出两个例子：先 send 后 receive 可顺利运行；但若三个进程都先执行 receive 操作，再执行 send 操作，每个进程都在等待其他进程发来消息，而其他进程也在等待接收消息，不会执行 send，导致所有进程永远阻塞在 receive 上，形成死锁。

---

**6. 系统中有 3 个进程，每个进程需要 4 个同类临界资源，保证系统不死锁的最小资源数是：**   
A. 10  
B. 11  
C. 12  
D. 13

**答案：A**  
**详解：**   
文档中给出公式：确保系统不出现死锁的最小资源数为  **(k-1)m + 1**。  
此处 m=3, k=4，代入得 (4-1)×3 + 1 = 3×3 + 1 = 10。  
推导思路：最坏情况每个进程分配到 (k-1)=3 个资源，此时还需要 1 个额外资源使某个进程完成任务并释放全部资源。

---

### 二、判断题

**7. 不可抢占资源被分配给进程后，操作系统可以在需要时强制收回，以便分配给其他进程。 （ ）**   
**答案：×**   
**详解：**   
文档定义：“不可抢占资源是指，一旦系统把这类资源分配给某进程后，就不能将它强行收回，而只能在进程用完后等待其自行释放。” 因此操作系统不能强制收回不可抢占资源。

---

**8. 如果系统中所有进程都先申请资源、再运行，执行完毕后才释放资源，一定不会发生死锁。 （ ）**   
**答案：×**   
**详解：**   
这其实就是打破“持有并等待”条件的一种方法，如果一次性申请所有资源，理论上不会产生死锁。但文档中的例子显示：如果进程申请资源的顺序不当（如 P1 先开 F1 再开 F2，P2 先开 F2 再开 F1），即使都是申请，也可能在持有部分资源后请求被其他进程占用的资源，从而死锁。所以“先申请资源”并不能保证安全，除非是一次性申请全部所需资源。

---

**9. 进程推进顺序不当是引起死锁的一个重要因素，文档中的两个例子都涉及该因素。 （ ）**   
**答案：√**   
**详解：**   
文档原文：“进程推进顺序不当，也是产生死锁的一个重要因素，上述的两个例子中都体现了该因素。” 无论是不可抢占资源的竞争例子，还是可消耗资源的消息通信例子，都是因为并发进程的推进顺序（交错执行时机）不当而导致了死锁。

---

### 三、简答题

**10. 简述不可抢占资源与可消耗资源在引起死锁时的不同机制。**   
**答案要点：**

- **不可抢占资源**：每个进程占有某些资源后，又去申请其他进程已占有的资源，而进程不能强行夺取对方资源，只能等待对方释放；由于对方也在等待，形成循环等待而死锁。典型例子：P1 占 F1 请求 F2，P2 占 F2 请求 F1。
- **可消耗资源**：资源是动态创建和消耗的（如消息）。进程在等待一条尚未产生且永远不会产生的消息时阻塞，而能够产生消息的进程又因为先执行了 receive 而陷入等待，所有进程都无法推进，形成死锁。

两者核心都是循环等待，但不可抢占资源竞争的是静态存在的设备或数据，可消耗资源竞争的是动态消息的发送与接收顺序。

---

**11. 用文档中 P1、P2 打开文件的例子，解释进程推进顺序如何影响死锁的产生。**   
**答案要点：**   
如果进程按顺序执行（P1 全部完成后再运行 P2），不会发生死锁，因为 P1 会释放文件，P2 随后可以获取。但如果 P1 打开 F1 的同时 P2 打开 F2，然后两者分别请求对方已占有的文件，由于两个进程都阻塞等待对方释放，而谁也无法释放，从而形成死锁。这体现了并发进程交替执行的推进顺序（交错）是导致死锁的关键因素之一。

---

### 四、计算题

**12. 系统中有 m 个进程，每个进程需要 k 个同类资源，每个进程采用“申请一部分资源、运行、再申请剩余资源”的方式。若系统总资源数为 R，请推导系统不死锁所需的最小 R 值，并写出推导过程。**   
**答案详解：**   
考虑最极端情况：所有进程都获得了 (k-1) 个资源，而系统已经没有多余资源可用。此时每个进程都还需要 1 个资源才能完成任务，但谁也无法得到，形成死锁。  
为了避免死锁，系统至少要多出 1 个资源，使其中一个进程能得到第 k 个资源，从而运行完毕并释放所占用的 k 个资源，随后这些资源可以分配给其他进程继续运行。  
因此不死锁的最小资源数 = m × (k-1) + 1。  
即 **R_min = m(k-1) + 1**。

---

以上题目覆盖了文档中死锁产生原因的三个主要方面，如有需要可以继续出题或深入讲解某一具体例子。

---

👤 **User**

第一题的可消耗资源是指什么

---

🤖 **Assistant**

根据文档《二、死锁产生的原因》中的定义：

---

### 可消耗资源

**定义：**  可消耗资源是指进程运行期间**动态创建和消耗**的资源，通常由**生产者进程创建、由消费者进程消耗**。

**最典型的例子：**  用于进程间通信的**消息**。

---

### 文档中的具体例子

三个进程 P1、P2、P3 通过消息机制通信：

- P1 产生消息 m1，发送给 P2
- P2 产生消息 m2，发送给 P3
- P3 产生消息 m3，发送给 P1

消息 m1、m2、m3 就是可消耗资源——它们由发送方进程创建（生产者），由接收方进程消耗（消费者），一旦被接收消费就不复存在。

---

### 与不可抢占资源的区别

|对比维度|不可抢占资源|可消耗资源|
| ----------| ----------------------------------| --------------------------------|
|存在方式|系统中固定存在的设备/数据|运行期间动态创建和消耗|
|典型例子|打印机、刻录机、磁带机、文件|消息、信号量等|
|释放方式|进程用完后自行释放|被消费者接收后消耗消失|
|死锁场景|进程持有并等待对方占有的设备资源|进程等待一条永远不会发出的消息|

---

**简单来说：**  可消耗资源不是系统里固定存在的硬件设备，而是进程在运行过程中临时创建、供其他进程使用的"一次性"资源，用完了就消失了。消息就是最常见的可消耗资源。

---

👤 **User**

同上 出题详解

---

🤖 **Assistant**

根据文档《三、死锁产生的必要条件》，我为你设计了以下题目并附上详细解答。

---

### 一、单选题

**1. 死锁的产生必须同时满足几个必要条件？**   
A. 2个  
B. 3个  
C. 4个  
D. 5个

**答案：C**  
**详解：**   
文档明确指出死锁的产生必须同时满足四个必要条件：互斥条件、请求和保持条件、不可抢占条件、循环等待条件。缺少任何一个都不会发生死锁。

---

**2. “一个资源被分配给某个进程后，其他进程不能强行夺走，只能由该进程主动释放”描述的是哪个必要条件？**   
A. 互斥条件  
B. 请求和保持条件  
C. 不可抢占条件  
D. 循环等待条件

**答案：C**  
**详解：**   
不可抢占条件的定义是：“一旦一个进程占有一个资源……那么该资源就不能被强行抢占，只能由该进程主动释放。” 这与题目描述完全一致。

---

**3. 如果一个进程已经占有了资源，又请求新的资源，且新资源已被其他进程占有，此时该进程会阻塞但又不释放已占有资源。这符合哪个条件？**   
A. 互斥条件  
B. 请求和保持条件  
C. 不可抢占条件  
D. 循环等待条件

**答案：B**  
**详解：**   
请求和保持条件定义为：“进程已经占有至少一个资源，但又提出了新的资源请求，而该资源已被其他进程占有，此时进程需阻塞，且不会释放已占有的资源。” 这正是此情景。

---

**4. 关于循环等待条件，以下说法正确的是：**   
A. 循环等待链的形成一定表明系统发生了死锁  
B. 循环等待条件完全独立于前三个条件  
C. 发生死锁时必然存在进程资源请求的循环等待链  
D. 循环等待只涉及两个进程

**答案：C**  
**详解：**   
文档指出：“系统发生死锁时，一定存在一条进程请求资源的循环等待链。” 选项A错误：循环等待链出现不一定死锁（满足四个条件只是可能死锁，不一定死锁）。选项B错误：文档说循环等待条件包含了前三个条件。选项D错误：循环等待链可以涉及多个进程，不一定只有两个。

---

**5. 关于死锁的四个必要条件，下列说法正确的是：**   
A. 只要四个条件同时满足，系统就一定会发生死锁  
B. 不满足互斥条件也可能发生死锁  
C. 如果打破了请求和保持条件，死锁一定不会发生  
D. 循环等待条件与互斥条件无关

**答案：C**  
**详解：**   
文档强调：“满足这些条件不一定发生死锁，但不满足这些条件一定发生不了死锁。”因此A错误。B错误，缺少任何一个条件死锁都不会发生。C正确：打破任何一个必要条件，死锁就不会发生。D错误，循环等待条件包含了互斥条件等前三个条件。

---

**6. 系统中有三个并发进程，每个都需要四个同类资源，则该系统必然不会发生死锁的最少资源数是：**   
A. 9  
B. 10  
C. 11  
D. 12

**答案：B（10）**   
**详解：**   
此题为文档中的练习。利用公式（k-1）× m + 1，其中 k=4（每个进程所需资源数），m=3（进程数），计算得 (4-1)×3+1 = 9+1 = 10。推导：每个进程都持有3个资源时系统资源耗尽（共需9个），此时再增加1个资源就能让某个进程运行完毕并释放全部资源，从而不死锁。

---

### 二、判断题

**7. 死锁的四个必要条件彼此完全独立，没有重叠关系。 （ ）**   
**答案：×**   
**详解：**   
文档明确指出这四个必要条件并非相互孤立，只是强调的内容不同，例如循环等待条件实际上就包含了前三个条件。因此它们之间存在关联，并非完全独立。

---

**8. 如果系统不存在循环等待链，那么即使满足其他三个条件，也不会发生死锁。 （ ）**   
**答案：√**   
**详解：**   
死锁必须同时满足四个必要条件。若没有循环等待条件（即打破了循环等待），则死锁一定不会发生，因为四个条件缺一不可。

---

**9. “请求和保持条件”意味着进程在请求新资源失败时，必须释放自己已占有的所有资源。 （ ）**   
**答案：×**   
**详解：**   
请求和保持条件的定义是进程在请求新资源失败时会阻塞，但**不会释放**已占有的资源。这正是死锁的危险所在——占着不放还等着别人释放。

---

**10. 打印机这种独占设备天然满足死锁的“互斥条件”。 （ ）**   
**答案：√**   
**详解：**   
互斥条件要求资源在一段时间内只能被一个进程使用。打印机一次只能为一个进程打印，完全符合互斥条件的描述，因此其天然具备互斥特性。

---

### 三、简答题

**11. 简述死锁的四个必要条件，并各举一个违反该条件从而可能避免死锁的方法。**   
**答案要点：**

- **互斥条件**：资源必须独占使用。破坏方法：使用可共享资源（如只读文件），或通过虚拟化技术让资源可同时访问（如打印机 spooling 技术）。
- **请求和保持条件**：进程已占有资源，又请求新资源，失败时不会释放已占有资源。破坏方法：要求进程一次性申请所有所需资源，或当请求失败时释放所有已占有资源。
- **不可抢占条件**：已分配资源不能被强行夺走。破坏方法：允许系统根据需要抢占资源（如剥夺调度），但需处理进程回退。
- **循环等待条件**：存在进程资源循环等待链。破坏方法：对资源进行全局编号，要求进程按顺序申请资源。

---

**12. 如何理解“满足四个必要条件不一定发生死锁，但不满足就一定不会发生死锁”？**   
**答案要点：**   
四个条件同时满足是死锁发生的必要前提，但死锁还需要特定的进程推进顺序才会触发，所以即使条件全部具备，系统也可能因运行顺序不同而安全完成。反过来，如果任何一个条件被破坏，死锁的“土壤”就不存在，系统无论如何调度都不会进入死锁状态。

---

**13. 系统中有 m 个进程，每个进程需要 k 个同类资源。请推导确保系统不出现死锁的最小资源数公式，并解释原理。**   
**答案详解：**   
参考前面题目推导：每个进程最多可申请 k 个资源。最坏情况：所有进程都获得了 (k-1) 个资源，系统资源恰好用完，每个进程都在等待第 k 个资源，但谁也得不到，于是死锁。此时消耗的资源数为 m×(k-1)。  
要打破僵局，系统只需多提供1个资源，让其中一个进程获得第 k 个资源并执行完毕，释放其所占有的 k 个资源，后续其他进程就能逐个完成。  
因此最小不死锁资源数 = **m(k-1) + 1**。

---

### 四、开放题

**14. 请结合死锁的四个必要条件，设计一个简单的并发程序场景（可以用伪代码描述），使其必然不会发生死锁，并解释你如何破坏了哪一个条件。**   
**参考答案思路：**   
（答案不唯一，只要合理破坏任一必要条件即可，例如破坏“请求和保持条件”）  
方案：要求每个线程在开始前一次性申请所有需要的锁（或资源），如果无法全部获得则释放已经获得的锁，然后重新尝试。这样可避免持有部分资源时又等待其他资源。伪代码示例：

```c
while(true) {
    acquire_lock(L1);
    if (!try_acquire(L2)) {
        release(L1);
        continue; // 回去重新申请
    }
    // 临界区
    release(L2);
    release(L1);
    break;
}
```

**解释**：当请求 L2 失败时，主动释放已占用的 L1，这破坏了“请求和保持条件”（因为不再“占着不放”），从而避免死锁。

---

👤 **User**

死锁 -> 四条件 四条件 不一定->死锁 对吗

---

🤖 **Assistant**

你的理解是**完全正确**的。用逻辑关系来梳理就是：

---

### 逻辑关系梳理

**1. 死锁 → 四个必要条件同时成立（✅ 必然）**

- 只要发生了死锁，往回追溯，一定能找到这四个必要条件同时存在。
- 也就是说：**死锁是四条件同时满足的充分条件**。

---

**2. 四个必要条件同时成立 → 死锁（❌ 不一定）**

- 文档原文：“满足这些条件**不一定**发生死锁，但不满足这些条件**一定**发生不了死锁。”
- 也就是说：四个条件是死锁的**必要条件**，但不是**充分条件**。
- 四个条件同时成立只是提供了死锁发生的“土壤”，但还需要特定的进程推进顺序（即“导火索”）才会真正触发死锁。

---

### 类比理解

|场景|死锁必要条件|死锁本身|
| ------| ------------------------------------| --------------|
|火灾|有可燃物、氧气、达到燃点|真正着火|
|死锁|互斥、持有等待、不可抢占、循环等待|所有进程阻塞|

有可燃物、氧气、温度够高，不一定就着火（还需要有人点火）。但如果没有可燃物，肯定着不了火。同理：

- **有四条件不一定死锁**（还缺“点火”——特定的推进顺序）
- **没有四个条件之一，绝对不死锁**

---

### 一句话总结

> **死锁必然伴随四个必要条件，但四个必要条件同时存在并不必然导致死锁。**

---

👤 **User**

出题考察 题目和详解分开

---

🤖 **Assistant**

## 题目部分

### 一、单选题

1. **死锁预防的基本思路是？II**  
   A. 检测死锁并恢复  
   B. 破坏死锁四个必要条件中的一个或多个  
   C. 允许死锁发生但不影响系统  
   D. 提高进程优先级
2. **SPOOLing 技术能预防死锁，主要是破坏了哪一个必要条件？I**  
   A. 互斥条件  
   B. 请求和保持条件  
   C. 不可抢占条件  
   D. 循环等待条件
3. **第一种协议（一次性申请全部资源）主要破坏了死锁的哪个必要条件？II**  
   A. 互斥条件  
   B. 请求和保持条件  
   C. 不可抢占条件  
   D. 循环等待条件
4. **第二种协议与第一种协议相比，主要优点是？II**  
   A. 完全避免了饥饿  
   B. 提高了资源利用率并减少饥饿  
   C. 实现更简单  
   D. 保证了进程顺序执行
5. **若一个进程申请新资源失败就释放所有已占有的资源，这种做法破坏的是？II** **III**  
   A. 互斥条件  
   B. 请求和保持条件  
   C. 不可抢占条件  
   D. 循环等待条件

   文档指出，当进程申请新资源失败时必须释放所有已保持资源，这相当于允许系统强行抢占进程已占用的资源，破坏的是“不可抢占条件”。
6. **按资源编号递增顺序申请资源，破坏了死锁的哪个必要条件？IV**  
   A. 互斥条件  
   B. 请求和保持条件  
   C. 不可抢占条件  
   D. 循环等待条件
7. **关于按资源编号顺序申请资源的方法，以下说法正确的是？IV**  
   A. 用户编程非常自由  
   B. 不会出现资源浪费  
   C. 系统无法增加新设备  
   D. 可能出现作业使用顺序与规定顺序不同导致资源浪费
8. **哲学家进餐问题中，若所有哲学家都先拿左边筷子再拿右边筷子，可能死锁的根本原因是？II** **IV**  
   A. 互斥条件  
   B. 请求和保持条件  
   C. 不可抢占条件  
   D. 满足死锁四个必要条件且形成循环等待链
9. **解决哲学家进餐问题的方法中，“最多允许4位哲学家同时拿筷子”破坏了哪个必要条件？IV**  
   A. 互斥条件  
   B. 请求和保持条件  
   C. 不可抢占条件  
   D. 循环等待条件
10. **在哲学家进餐问题中，规定奇数号先拿左边、偶数号先拿右边，不会死锁是因为？III**  
    A. 只有一个人吃饭  
    B. 抢不到筷子就放手  
    C. 破坏了循环等待条件  
    D. 筷子数量增加了

---

### 二、判断题

11. **死锁预防是动态解决死锁的方法，在系统运行时检测死锁并恢复。（ ）N**
12. **破坏互斥条件是一种适用范围很广、容易实现的死锁预防方法。（ ）N**
13. **第二种协议允许进程在运行开始时只申请初期需要的资源，运行中再逐渐释放和申请新资源。（ ）Y**
14. **破坏不可抢占条件的方法实现简单，代价小，特别适合打印机这类设备。（ ）N**
15. **若不存在循环等待链，则死锁一定不会发生。（ ）Y** **N 逆否命题**
16. **哲学家进餐问题中，5 位哲学家同时拿左边筷子就会死锁，说明死锁一定发生。（ ）N**

---

### 三、简答题

17. **简述死锁预防的四种破坏策略，并各举一例说明。**

    破坏互斥：spooling代理打印机请求排队  
    破坏请求保持：提前申请资源 过程中不再申请资源  
    破坏不可抢占：要求新的资源时得不到满足就释放已有资源  
    破坏循环等待：只能请求资源编号大于持有编号的资源
18. **解释第一种协议和第二种协议的原理，比较它们的优缺点。**

    获得所有资源开始 vs 先获得初期资源 一边放一边要新的
19. **为什么按资源编号顺序申请资源可以破坏循环等待条件？它有哪些实际限制？**

    一个圈五号哲学家不能拿一号哲学家的筷子，就不会循环等待

    设备顺序需固定、作业顺序可能冲突、编程受约束。
20. **哲学家进餐问题中，原始解法为什么会死锁？请给出至少两种解决方案并说明破坏了哪个条件。**

    循环等待

    解决1：最多允许4人拿筷子（破坏循环等待）。  
    解决2：奇数号先左后右，偶数号先右后左（破坏循环等待）。  
    解决3：同时能拿到两根时才允许拿（破坏请求与保持）

---

### 四、分析题

21. **某并发程序中，进程 P1 和 P2 分别需要资源 A 和 B：**

```
P1: 申请 A; 申请 B; 释放 B; 释放 A;
P2: 申请 B; 申请 A; 释放 A; 释放 B;
```

**请问这段代码可能死锁吗？如果会，请指出死锁的情形，并基于死锁预防的四种策略，各提出一种修改方案来预防死锁。**

当然会 P1持有A P2持有B P1申请B P2申请A

- 破坏互斥：不合适（资源A、B可能必须互斥）。
- 破坏请求和保持：要求一次性申请A和B，或申请不到就释放已持有的。
- 破坏不可抢占：如果申请B失败，让P1释放A；或者让系统强行抢走P2的B分配给P1。
- 破坏循环等待：定义资源编号（A=1, B=2），都先申请编号小的，P2必须按顺序先A后B，与P1统一为 A→B，循环消失。

22. **系统中有 5 个进程，每个进程需要 3 个同类资源。若采用“一次性申请全部资源”的方法，至少需要多少个资源才能确保不出现死锁？若采用“按编号顺序”方法，资源数同样是多少？两种方法在资源利用率上的区别如何？**

    3个；3个；不知道 （答案不一定对 不太严谨的题）

    一次性申请全部资源的最小不死锁资源数 = 5×(3-1)+1 = 11。  
    按编号顺序的方法理论上资源数与一次性全申请所需最小资源数可以相同，均为至少满足最坏情况下每人缺1个资源时有额外1个资源解围，所以也为11。  
    区别：一次性全申请方法资源闲置时间长，利用率低；按编号顺序方法资源按需逐步分配，利用率较高。

---

‍

## 答案与详解

### 选择题详解

**1.**   
**答案：B**  
文档指出死锁预防是“通过破坏四个必要条件中的一个或多个来防止死锁的发生”。

**2.**   
**答案：A**  
SPOOLing 技术为打印机设置一个代理进程，其他进程不直接竞争打印机，从而让互斥条件形同虚设，即破坏互斥条件。文档中强调“只要只有一个进程访问临界资源就没必要保证互斥访问”。

**3.**   
**答案：B**  
第一种协议要求进程一次性申请所有资源，如果申请不到就不占有任何资源等待，从而破坏了“请求”和“保持”条件（即请求和保持条件）。文档明确提到“破坏了‘请求’条件”、“破坏了‘保持’条件”。

**4.**   
**答案：B**  
第二种协议是对第一种的改进，进程逐步申请和释放资源，减少了资源闲置时间，提高了利用率，也降低了饥饿概率。文档举例说明第二种协议“使进程更快完成任务，提高设备利用率，减少饥饿”。

**5.**   
**答案：C**  
文档指出，当进程申请新资源失败时必须释放所有已保持资源，这相当于允许系统强行抢占进程已占用的资源，破坏的是“不可抢占条件”。

**6.**   
**答案：D**  
按编号递增顺序申请资源可防止循环等待环的产生，因此破坏的是循环等待条件。

**7.**   
**答案：D**  
文档说明按编号顺序申请资源的方法存在缺点：新增设备受限制、作业使用顺序与规定顺序可能不同导致浪费、限制编程灵活性。并非完全无浪费或编程自由，因此选 D。

**8.**   
**答案：D**  
哲学家进餐原始解法中，资源（筷子）独占（互斥），拿到一根后不放下（请求与保持），筷子不能强行夺走（不可抢占），以及可能形成每人拿左边一只并等待右边的环（循环等待），全部条件满足且进程推进顺序恰当时就发生死锁。

**9.**   
**答案：D**  
用 `priority = 4` 限制同时拿筷子的哲学家数量，使得至少一人能同时取得两只筷子，避免形成环形等待链，破坏了循环等待条件。

**10.**   
**答案：C**  
不同编号的哲学家拿筷子的顺序相反，打破了循环等待环，因此破坏了循环等待条件。

---

### 判断题详解

**11.**   
**答案：×**   
死锁预防是静态方法，在系统运行前就通过规则避免死锁；动态检测和恢复属于死锁避免或死锁检测与恢复，不是预防。

**12.**   
**答案：×**   
破坏互斥条件适用面很窄，只适用于少数可以虚拟化的设备（如打印机），并非适用范围广且易实现。

**13.**   
**答案：√**   
第二种协议正是允许进程开始运行时只申请初期需要的资源，边运行边释放再申请新资源。

**14.**   
**答案：×**   
破坏不可抢占条件的方法实现复杂，代价大，因为强行抢占可能导致进程前期工作失效，如刻录机中途被抢会刻废光盘。

**15.**   
**答案：√**   
循环等待是死锁的必要条件，缺其一则死锁必不发生。

**16.**   
**答案：×**   
五个哲学家同时拿左边筷子只是可能死锁，但不一定每次都发生，取决于并发执行的具体推进顺序。

---

### 简答题要点

**17.**

- **破坏互斥条件**：例如用 SPOOLing 将打印机变成逻辑共享，仅有代理进程访问。
- **破坏请求和保持条件**：一次性申请所需全部资源（第一种协议）或边运行边释放再申请新资源（第二种协议）。
- **破坏不可抢占条件**：申请新资源失败时释放所有已持资源。
- **破坏循环等待条件**：按资源编号递增顺序申请资源。

**18.**   
第一种协议：运行前一次性申请全部所需资源，缺点资源利用率低、容易饥饿；优点简单安全。  
第二种协议：开始只申请初期资源，逐步释放后再申请，提高资源利用率并减少饥饿。缺点实现稍复杂。

**19.**   
因为按递增顺序申请，不可能存在一个高编号进程请求低编号资源的同时，低编号进程又请求高编号资源，因而无法形成循环等待环。限制：设备顺序需固定、作业顺序可能冲突、编程受约束。

**20.**   
原始解法死锁原因：五人同时拿左边筷子，每人持有一根并等待右边，形成循环等待。  
解决1：最多允许4人拿筷子（破坏循环等待）。  
解决2：奇数号先左后右，偶数号先右后左（破坏循环等待）。  
解决3：同时能拿到两根时才允许拿（破坏请求与保持）。

---

### 分析题要点

**21.**   
会死锁：P1持有A等待B，P2持有B等待A。  
预防方案示例：

- 破坏互斥：不合适（资源A、B可能必须互斥）。
- 破坏请求和保持：要求一次性申请A和B，或申请不到就释放已持有的。
- 破坏不可抢占：如果申请B失败，让P1释放A；或者让系统强行抢走P2的B分配给P1。
- 破坏循环等待：定义资源编号（A=1, B=2），都先申请编号小的，P2必须按顺序先A后B，与P1统一为 A→B，循环消失。

**22.**   
一次性申请全部资源的最小不死锁资源数 = 5×(3-1)+1 = 11。  
按编号顺序的方法理论上资源数与一次性全申请所需最小资源数可以相同，均为至少满足最坏情况下每人缺1个资源时有额外1个资源解围，所以也为11。  
区别：一次性全申请方法资源闲置时间长，利用率低；按编号顺序方法资源按需逐步分配，利用率较高。
